# Risk R028: The register's scaffold path is mechanical and its curation path is not

**Status**: Active
**Category**: delivery — governance record
**Identified**: 2026-08-05
**Owner**: addressr-maintainer
**Last reviewed**: 2026-08-05
**Next review**: 2027-02-05

## Description

New register entries arrive **mechanically**: a `wr-risk-scorer:pipeline` `RISK_REGISTER_HINT` becomes a `docs/risks/R<NNN>-<slug>.active.md` with the ADR-026 ungrounded-output sentinel in every scoring field, via the Phase 2b drain (`wr-risk-scorer` ADR-056). Curating one is entirely **by hand**: there is no curate/update skill, and `create-risk` mints a new ID with no path for editing an existing entry, so P079's instruction to "route via `/wr-risk-scorer:create-risk` rather than editing scoring fields by hand" is unsatisfiable as written.

An automated inflow against a manual outflow drifts in two directions, and nothing caught either:

- **Re-accumulation.** Ungrounded entries pile up at a rate set by scorer activity rather than maintainer decision. That is what P083 was opened about — 24 of 25 entries uncurated, with every risk assessment reporting "no lifetime baseline to reconcile against", sixteen times in one session.
- **Self-disagreement.** Curated entries drift against their own index rows and against their own prose, because every number is written by hand and nothing recomputes it.

This is not the same risk as [R027](R027-deferred-integration-accumulates-unpriced-risk.active.md). R027 prices an _action_ scored against an unscored baseline; R028 is a _register_ that decays between scored actions. Distinct mechanisms.

### Measured at the end of the drain that was supposed to have finished

Every one of these was live on 2026-08-05 after P083's seventh and final batch declared the register curated:

| Drift                                                                                                                            | Count |
| -------------------------------------------------------------------------------------------------------------------------------- | ----- |
| Contradictory above-appetite counts in one document (`six of fifteen` vs the computed `ten of 15`)                               | 2     |
| Entries carrying a duplicated `## Change Log` stanza                                                                             | 5     |
| Curated entries asserting in present tense that their own grounded fields were ungrounded                                        | 9     |
| Descriptions quoting a residual their own Change Log had superseded                                                              | 2     |
| Unchecked task asserting "~15 sentinel-bearing entries remain", directly above a section declaring zero                          | 1     |
| A close-out count not recomputed after a score moved ("ten of 16 above appetite" after R028 itself went 2 → 6, making it eleven) | 1     |

The counts matter more than any single instance: this is what one batch of hand-maintenance produced **while the maintainer was actively watching for exactly this class of error**, having already caught and corrected one instance of it in the same sitting.

## Inherent Risk

Impact × Likelihood _before_ controls.

> **Scale interpretation.** The policy's likelihood descriptors are written about _changes_. This is a standing posture, so likelihood reads as the probability of the condition materialising in a given period — the R010 precedent.

- **Impact**: 2 (Minor) — no runtime, publish or consumer path. The damage lands on the governance surface: `docs/risks/` is read by the ADR-059 catalog protocol on **every future scored action**, so a stale entry or a wrong score degrades every subsequent baseline. Not Impact 1, because this is a machine-consumed surface rather than inert prose.
- **Likelihood**: 4 (Likely) — an observed base rate, not a projection. **Twenty** instances in the batch tabled above.
- **Inherent Score**: 8
- **Inherent Band**: Medium

## Controls

- **`test/js/__tests__/risk-register-invariants.test.mjs`** — **evidenced**, runs in the unit suite, and mutation-tested at introduction (understating R006's residual in the index from 10 to 5 made it fail and name that exact cell). It asserts **thirteen** invariants: no active entry carries the sentinel; no curated entry claims in present tense that its fields are ungrounded; every entry has exactly one Change Log; every README Register row's inherent and residual match the entry's own scores; this entry's cited drift-table total matches the table's own Count column; this entry's stated check-count matches the test file; any document stating the above-appetite partition in the exact bolded digit form agrees with the entries; no entry refers to a check by position — **widened 2026-08-05** from `invariant <digit>` to cover ordinal words and the noun "check", because `The twelfth check` is the same defect and goes silently wrong the moment a check lands ahead of position twelve, which is the argument the rule is made from; dated bullets exempt; every cited residual figure matches that entry's own score cell; no referring document predates the entry it describes — **widened 2026-08-09 to walk committed history**, dating an entry at its last change OUTSIDE the Change Log rather than at its last touch, because the pre-existing Change-Log-only exemption was scoped to the dirty working tree and so bought convergence only within a sitting; no entry contradicts a fact it declares canonical; every stated push-tier apply count agrees with R021's canonical `Metrics` cell — **configured narrowing stated, because every check here carries one**: the cardinal must be a numeral bound to `production applies` / `successful applies` / `fired on production N times`, dated `- YYYY-MM-DD:` bullets and lines scoped `as at`/`as of` are exempt per the rule above, **code spans are exempt** — load-bearing, and relied on in the batch that landed this check, where a Change Log bullet quotes a count as a regex test case — the canonical cell itself must carry the count as `**Canonical count: <numeral>**` — **narrowed 2026-08-09**, because the extractor previously read the count out of the phrase `<count>, all successful` and so encoded a claim about outcomes that the register must stay free to change; it failed with "must state the canonical apply count" against a cell that stated it plainly, the moment an apply failed; and the scan root is `docs/`; and the index covers every active and retired file.

  The checks are named, never numbered **in live prose** — dated records keep their contemporaneous form, which is the same exemption the fence and the apply-count check carry. An earlier version stated this as an absolute, contradicted two sections down by a dated bullet that numbers one, and that is the name-wider-than-configured-value error one paragraph above the disclosure correcting it. An earlier draft referred to checks by position in two places, with different literals and different fates: one named the README-row check, which was fourth then and is fourth now; the other named the partition check, which was accurate when written and went wrong only when a later check landed ahead of it. That is the whole argument for naming — a positional reference can be correct on the day and false by the next edit, with nothing signalling the change.

  It earned its keep on introduction, finding a duplicated Change Log in a retired entry that the by-hand sweep of the same defect had missed.

  **Two checks added 2026-08-05 reach the two cross-file moves of the miss** — a stale figure cited across documents, and a referring entry left behind when the entry it describes moves. **Not "the halves where every observed instance lived"**, which is how an earlier version of this sentence read: that is the coverage claim the Change Log below records withdrawing, and it is contradicted by this entry's own note that the citation check covers one phrasing of several. The re-accumulation half is closed by the sentinel check, not by either of these. The score-citation check asserts that any `R<NNN> at <N>` citation under `docs/` equals that entry's own cell. **That is its configured form, and it is narrower than "any residual figure cited in prose"** — an earlier version of this bullet claimed the wider scope. Citations written as `12 inherent / 8 residual` or `residual 10/25` are invisible to it, and only ID runs joined by `/`, `,` or `and` are covered — arithmetic that happens to live in prose, one level out from the appetite partition, and mutation-proven on the instance that prompted it: P083 carried `R028 at 6` after this entry's residual had moved to 8. The review-fence check asserts that no **active** entry making an **ID-token** reference to another active entry predates that entry's last change. Stating it that way on purpose: "no entry referring to another" is the name, and the configured value is narrower on three axes — **active referrers against active-or-retired targets**, `R<NNN>` token only, and code-span exempt. An earlier version of this sentence said "active-to-active only", which was a surviving fragment of the withdrawn active-only draft and was contradicted both by the test and by the bold text fifteen lines below it. A reference written in the subject's vocabulary rather than its identifier is invisible to it, which is not a hypothetical. Until this batch, R010 credited the `/health`-probing rolling deploy as bounding a bad flip to a degraded subset while R006 — the register's highest residual — described that same wire draining the pool with the mechanism feeding itself, and **R010 carried no `R006` token anywhere**, which is precisely what made the conflict unreachable. It was found by the risk scorer reading both. Both entries now cite the other, R010's bullet is demoted to listed-not-credited, and the contested residue is narrowed to the reachable-but-slow sub-case and priced on R006. Naming the gap rather than the name is the discipline this entry records itself failing twice — where "referring" excludes what is **structurally quoted** — a backticked ID or a blockquote line — and nothing else. A middle draft excluded the whole `## Change Log` section instead, on the theory that such mentions are audit-trail prose. That was wrong three ways, and the scorer found all three: it closed **none** of the ten stale pairs it was taken to close, because every one was body-resident; it unsaw a real live claim, since R022's only reference to this entry is a present-tense assertion about its Controls section living in R022's Change Log; and its own in-file exemplar was a phrase that sits in this entry's Controls, not in any Change Log. A region marker sixty lines away says nothing about whether a sentence asserts about now; a backtick sits at the claim. That distinction also matters because the register's remedy for a fence failure is to write a verification bullet — into exactly the section the region-exclusion would have blinded. Its scope is **asymmetric**, and the asymmetry is the point. **Targets are active ∪ retired**: a retired file's timestamp is frozen, so it flags each stale active referrer once and then stays green permanently — bounded by construction — and retirement is the largest state change an entry undergoes, so an active entry still describing a retired one as live is exactly what this is for. Under the active-only scope an earlier draft shipped, the `git mv` that retires an entry was the one change that dropped it from the fenced set. **Referrers stay active-only**: a retired referrer is permanently older than anything that moves after it, so it re-flags forever with a null timestamp-bump as its only remedy — and a check whose remedy is a no-op does not get waived, it gets _performed_, which turns a signal into a ritual. Retired records are kept honest by date-scoping their claims when written. That distinction was learned the expensive way: a symmetric widening flagged 25 pairs in one run, and the draft that followed cut both axes the same way and lost the bounded half. It did find R025 asserting a `deploy_only` path had never been dispatched three days after it had been, which is now dated accordingly.

  The check is keyed on **git commit order** rather than the `Last reviewed` field, because that field cannot order two edits made on the same day — which is precisely the case that slipped when R015 was curated the same day R020 moved. Files with uncommitted edits count as current, so a batch that touches an entry and its referrers together passes by construction.

  **It also closes the re-accumulation half, as a side effect worth naming**: a freshly scaffolded entry carries the sentinel, so the next Phase 2b drain will **red the build** until the entry is curated. That converts silent accumulation into a loud stop. It is the right default — an ungrounded entry in a machine-consumed catalog is a defect — but it means the drain can no longer land entries unattended, which is a consequence to accept deliberately rather than discover.

- **NOT a control: the maintainer recomputing before commit.** It has evidence of application — it caught one wrong count in this very batch — and it still missed a sibling instance of the identical sentence-shape two paragraphs below in the same file. A procedural check that demonstrably misses instances in the artefact it is checking cannot be credited, which is the discipline R003, R004, R010 and R022 each applied to their own procedural controls.

## Residual Risk

Impact × Likelihood _after_ controls.

- **Impact**: 2 (Minor) — unchanged; the control detects rather than prevents.
- **Likelihood**: 3 (Possible) — **4 → 3 on mechanism, not 4 → 2.** Every batch of register hand-maintenance to date had shipped prose drift, including every batch whose purpose was remediating it — an unbroken run with no counter-instance. The checks reach **two of the four** recorded moves outright — a stale figure cited across documents, and a referring entry left behind when the entry it describes moves. The apply-count check additionally **dents the register-scoped shape the fourth move predicted**, by giving one file-global fact a canonical cell; that is a dent in a predicted shape, not closure of a recorded move, which is why it does not change the count. The two intra-file moves — a fact restated across several sections in different wordings, so correcting one leaves the others — were **wholly unreached** until the canonical-state declaration landed, and are now **reachable on declaration**: an entry tables the fact, its value, and the phrasings that would contradict it, and the check enforces that. Mutation-proven at introduction — restoring R022's Status alone named the surviving contradiction against the declared cell. An earlier version of this sentence allocated five positions across a four-element set by calling the apply-count dent "part of a third". **Likelihood stays at 3, and reachable-on-declaration is why.** The shape works, but it is opt-in per fact, and nothing generates the list of facts that need one — the same caveat the dent carried, moved one step later in the pipeline rather than removed. Two check defects found and fixed in this same sitting are the live counter-evidence against dropping further: the fence's dirty-set parse trimmed the leading space off `git status --porcelain`'s first line, so the **first** dirty file silently fell out of the set on every run since the check landed — a check failing open on its first input — and the fence treated its own prescribed remedy (write a verification bullet) as a move, so every remedy re-armed the check one hop further out.

  A first draft claimed 2 and said of the checks "Both now red the build." Neither half of that survived review. The citation check covers **one phrasing** of several live in the very document it was built from. The review-fence check was **inert in the gate**: both jobs running `test:js` used a bare `actions/checkout`, so under `fetch-depth: 1` every `git log` lookup returned empty, every comparison became `0 < 0`, and the check passed having done no work. Both are now fixed — the separator accepts bare `and`, and the check fails loudly on a shallow clone with `fetch-depth: 0` set on both jobs — and the intra-file moves, which is where the sweep failed twice, are now reachable on declaration rather than unreached.

  Not 2, and not 1. The residue is narrative prose that carries a judgement rather than a number — a rationale gone stale against its own controls, a Change Log describing a correction inaccurately — and nothing mechanical reaches it. The instance count stays in the Change Log rather than in this field, because _a count in prose is the defect this entry is named after_ and a tally goes stale in both directions, on the next dirty batch and the next clean one alike.

  Corrected from 1, via 3. The first draft justified 1 with "a breach now fails CI on the commit that causes it, so neither drift direction can accumulate silently." That is true of cells and false of sentences, so it credited the control by its coverage-in-name rather than its configured coverage — the move R003's curation withdrew when it credited `create_before_destroy` and a `DeploymentPolicy` named "Rolling".

- **Residual Score**: 6
- **Residual Band**: Medium
- **Within appetite?**: **No** — appetite is 5 inclusive. One over, with a specific and small path to 4: **generate the declaration list rather than remember it.** The canonical-state block reaches the intra-file moves but only for facts a human thought to declare, which is the remembered step the whole entry is against. The closing move is a check that flags an entry restating the same figure-plus-noun across three or more sections with no canonical declaration covering it. Deliberately not built speculatively — it is a heuristic with a false-positive budget, and it should be justified by the next instance it would have caught, not by the argument that it would.

### What the control does not cover

**Free prose that is not arithmetic.** The invariants are structural — sentinels, headings, index rows, score cells, and (since the partition check) any document's claimed above-appetite partition. What they still cannot check is a sentence carrying a judgement rather than a number: a Description quoting a superseded residual in prose, a rationale that has gone stale against its own controls, a Change Log describing a correction inaccurately. Those occurred in the batch tabled above and were fixed by hand.

That is the residual's honest ceiling: the class of error that started this entry — **a number asserted rather than computed** — is closed wherever the number lives in a checkable cell, and open wherever it lives in a sentence.

## Treatment

**Mitigate.** The invariants test is the control. It does **not** yet bring the residual within appetite, but the earlier position — that the prose half was uncontrolled and the entry had no local path at all — was wrong twice over: it was the accept-in-disguise shape R027 refuses, and it under-read how much of the observed drift was computable. Two checks landed on the strength of that correction and took it 8 → 6. The remaining step is the two wholly-unreached intra-file moves, which is a defined piece of work rather than an appeal to more care.

The root cause is **not** fixed and is not this repo's to fix: an automated inflow against a manual outflow remains the shape until `wr-risk-scorer` ships a curate/update skill. What the test changes is that drift now stops the build instead of accumulating unobserved, which is the difference between a hazard that is bounded and one that is not.

**The local half, now landed.** An earlier draft of this entry declined to widen the test "toward prose" and named no local treatment at all — which would have left it above appetite with only someone else's action as its discharge. That is functionally accepted while labelled Mitigate, and [R027](R027-deferred-integration-accumulates-unpriced-risk.active.md) refuses exactly that shape in as many words: the register should not present an entry as locally unfixable while a local half is available.

The distinction the earlier draft missed is that the defect is not prose — it is **arithmetic that happens to live in prose**. `N of the M sit above appetite` is computable from the same score cells the README-row check already parses. So the partition check computes the appetite partition from the entries and asserts that any document under `docs/` claiming it agrees. Exact, structural, and no wider in scan root than it needs to be. It is mutation-proven on the instance that prompted it: reverting the count to "Ten of the 16" fails and names the file.

Still deliberately **not** treated by a general prose checker. That reasoning stands — an inexact governance test gets its failures waived, which costs more than the drift it catches. The partition check is exact; a prose checker would not be.

## Monitoring

- **Trigger to re-assess**: a Phase 2b drain scaffolds a new entry (the build will say so), or a scored action reports a missing catalog baseline. Deliberately **not** "a new pipeline hint with this slug" — that fires on scorer activity rather than on the hazard, and it is why the register slept through three cutovers (P083).
- **Discharge condition**: `wr-risk-scorer` ships a curate/update skill, at which point the inflow/outflow asymmetry itself is gone and this entry can retire rather than being carried indefinitely.
- **Metrics**: invariant-test failures per month, and the count of active entries carrying the sentinel at any time (expected: zero, enforced).

## Related

- Criteria: `RISK-POLICY.md`
- Realised-as: [P083](../problems/open/083-risk-register-is-an-index-of-hints-not-a-register-24-of-25-entries-uncurated.md) — the drain that surfaced both drift directions. This entry is the class P083 leaves behind after closing the instance.
- [P079](../problems/open/079-rollback-exercised-is-not-a-gate-on-warm-standby-decommission.md) — carries the "route via `create-risk`" instruction that is unsatisfiable for edits.
- [R027](R027-deferred-integration-accumulates-unpriced-risk.active.md) — adjacent, not overlapping: R027 is about how an _action_ is priced, this is about how the _register_ decays between actions.
- Precedent for the control: [R018](R018-adr-links-problem-ticket-committed-before-ticket-exists.active.md) — same move (structural invariant asserted in test, enumerating every offender in one failure) against the documentation-link class.
- Personas affected: [addressr-maintainer](../jtbd/addressr-maintainer/JTBD-400-ship-releases-reliably-from-trunk.validated.md)

## Change Log

- 2026-08-10 (fourth entry today): **Re-verified after R015's stale gate quote and line anchors were corrected.** **Direction: a THIRD confirming instance in one day, and the most useful one.** R015's quoted gate string and its four `release.yml` line anchors were stale — for the second time on the same date. The first correction re-quoted the gate and re-cited the lines; both rotted again within hours. The second correction DROPPED the line anchors entirely and replaced them with stable step names plus a `grep` recipe.

  **That is the generalisable move this entry has been circling.** A line-anchored citation in prose is not merely uncovered by the invariants suite — it is guaranteed to rot, because line numbers move on every edit to the cited file while the fact stays true. Naming the anchor (a step name, a load-bearing string) instead of the coordinate removes the rot rather than detecting it. Same shape as the ADR-032 smoke-probe criterion re-anchored on `grep -n "no-origin not permitted"` earlier today. No re-rate; recorded as a pattern worth applying wherever this entry's ceiling bites.

- 2026-08-10 (third entry today): **Re-verified after R020 was re-scored against the changesets-armed successor route**, which is now BUILT and is the sole path to a production infrastructure apply — `deploy_only` is deleted. **Direction: this entry's checks held on the fan-out and its ceiling held too.** The re-score of R020 fanned to seven referrers, all surfaced mechanically by the review fence. But note what the fence did NOT catch and could not: R020's canonical-state row asserted "route not yet built", which became false the moment the successor landed, and its Controls bullet described a test assertion that had already been converted. Both were caught by review, not by a check — the same prose-versus-checkable-cell split this entry records as its standing limit. No re-rate; a confirming observation.

- 2026-08-10 (second entry today): **Re-verified after [R020](R020-deploy-path-push-tier-prod-deploy-precondition-unmet.active.md)'s treatment and monitoring were re-pointed** off the `deploy_only` dispatch and onto the changeset-armed release-PR route, per the user-ratified JTBD-400 amendment in commit `09f6418`. **Direction: this is evidence AGAINST the checks being sufficient, and it belongs here rather than in the favourable entry below.** The drift itself — R020's Treatment proposing an exercise through a route Phase 4 deletes, and its Monitoring trigger naming an input that would no longer exist — was caught by the **user reading a status summary**, not by any mechanical check. Nothing in the suite relates a risk entry's Treatment prose to the JTBD corpus or to a planned deletion, so the register was free to keep recommending a route the project had already decided to remove. The referrer fence and the canonical-state matcher then worked exactly as designed on the fan-out (seven referrers surfaced, one candidate phrasing rejected for colliding with live prose) — but they operate one level below where this drift lived. The entry below records the checks catching a fan-out; this one records them missing the premise. Both are true and the second is the more useful. **The gap is specific and nameable**: an entry's Treatment can name a mechanism that is scheduled for deletion, and nothing fails. Worth mechanising if the same shape recurs; not worth building a corpus-to-register linter on one instance. No re-rate — the residual already prices unmechanised curation, and this is a confirming observation rather than a new hazard.

- 2026-08-10: **Revisited for the `deploy/**` push-axis retirement.** [R021](R021-push-tier-deploy-axis-arms-prod-terraform-apply.retired.md) and [R022](R022-unstaged-terraform-lockfile-drift-arms-deploy-axis.retired.md) retired (hazard deleted, not reduced); [R020](R020-deploy-path-push-tier-prod-deploy-precondition-unmet.active.md) re-scored 8 → 10 because retiring the axis deleted the ground its Impact 4 rested on. Governance: [ADR 001](../decisions/001-risk-gated-release-process.proposed.md) and [ADR 040](../decisions/040-release-pipeline-change-type-action-matrix.proposed.md), 2026-08-10 amendments. **Direction: this commit is a worked example of the mechanisation this entry tracks.** The retirement fanned out to the canonical-cell lookup, the Register/Retired tables, P083's above-appetite partition and seven referring entries — every one of which was surfaced by a mechanical check rather than by memory, including one that would have THROWN a TypeError and errored the whole suite. Recorded as evidence the checks are load-bearing. No re-rate.

- 2026-08-09 (third entry today): Re-verified after the push-tier axis fired again — run `31283258197` applied the `source_hash` hardening itself, taking the canonical apply count to six with five successful. The apply was `0 added, 1 changed, 0 destroyed`, an in-place update of `aws_s3_object.elasticapp` with its id unchanged, so the application version and `version_label` were untouched and the fleet did not cycle. Predicted from the pinned provider's schema before the push and matched exactly.

  **This entry's citation holds** — the count moved, not the ownership, and nothing here restates the cardinal. Recorded because R021 and R020 both took body edits at their count sites, so the fence correctly required their referrers in the same commit. Under the widened fence this bullet does not propagate further.

- 2026-08-09 (second entry today): Re-verified against R021's treatment ratification — preconditions hardened rather than a plan gate added or the residual accepted — and the `source_hash` control that landed with it. **This entry's citation holds**: the R021 references here are the canonical apply-count cell and dated records of this batch's own drift, none of which a treatment choice reaches. **R021's residual did not move**: it stays at 10 and above appetite, because Impact is fixed at 5 while nothing on that path reviews the plan.

- 2026-08-09: **The review fence was widened to committed history, and the reason is that its remedy has no fixed point.** The R021 re-rate batch turned trunk red on this check. R008 and R022 took Change-Log-only edits; committed, `git log -1` dated those as moves and flagged R009 and R027. Writing the prescribed verification bullets in those two, and committing them, flagged R007, R008 and this entry — all already inside the batch.

  **That is the difference between expensive and non-terminating, and this entry had recorded only the first.** The 2026-08-05 bullet on the bounded closure said ten of sixteen entries, one iteration deep, "and it terminated — which is what bounded means in practice". It terminated because every file was dirty in one sitting, and the dirty-case exemption made the remedy free. Across a commit boundary the exemption does not apply, the remedy is itself a Change Log edit, and the reference graph is **cyclic through this entry**, which nearly every entry cites. There is no ordering of bullets that converges. The bound was a property of the sitting, not of the closure.

  The fix takes the upgrade path the check's own `ponytail:` comment named: walk the commits touching a file, newest first, and date it at the first one that changed anything outside the Change Log. A verification bullet no longer moves an entry, so it no longer arms its referrers. Cost is 0.9s to 2.3s on the suite. **The check count is unchanged at thirteen** — an existing check was widened.

  **Mutation-proved in both directions, on committed state rather than a dirty tree** — which matters here more than usual, because a dirty-tree green is precisely the false signal that let the red reach trunk in the first place. A substantive sentence added to R021's body, committed with no referrer touched, reddens the check and names all eight referrers. A Change Log bullet added to the same entry, committed the same way, leaves it green. Both probes were reset afterwards; only their outcomes are kept. **This bullet is itself the third proof**: adding it edits nothing outside a Change Log, so it does not arm a single referrer, where before the widening it would have armed six.

  **A new dependency this created, named because it is a fail-open and it is not yet enforced.** `body()` splits at the first `## Change Log` heading, so it is correct only while that is the LAST section of every entry. Before the widening that convention was load-bearing on the dirty path alone, where a wrong answer mis-classified one working-tree edit. It is now the sole dating mechanism for every target on every run. If anything is ever appended below the Change Log — the Phase 2b drain auto-populates `## Evidence Log`, which sits above it in R021 only by convention — every body edit beneath that point becomes invisible and the fence silently stops arming. The convention holds today in `TEMPLATE.md` and in every entry checked. The blast radius grew with this change and the enforcement did not; asserting it inside the existing fence check, without adding a fourteenth, is the obvious next step and is deliberately not taken in the same commit that restores trunk.

  **What this does NOT fix, stated because the ceiling moved rather than vanished.** A real body edit still propagates to referrers, which is the check working. And the exemption is now keyed on "outside the Change Log", so a substantive claim written INTO a Change Log bullet is invisible to it — the dated-bullet rule makes that mostly safe, and it is the same seam this entry already records between structural checks and prose.

- 2026-08-09: Re-verified against R021's re-rate and R020's apply-count move to five, and **the canonical-cell check was widened in the same commit** — recorded here because this entry owns that check's description. The extractor read the canonical count out of the phrase `<count>, all successful`, so correcting R021's `Metrics` cell to record a failed apply made it find nothing and fail with "must state the canonical apply count" against a cell that stated it plainly. **A check whose extractor encodes a fact the register is meant to be free to change is a check that reddens on the truth**, and that is a distinct defect from the ones this entry catalogues: not a control credited for more than it reaches, but a control whose _reading_ of the canonical cell was entangled with a claim about outcomes. The count now sits in an explicit `**Canonical count: <numeral>**` token with the outcome breakdown as free prose beside it. The check count is unchanged at thirteen — this widened an existing check rather than adding one.

  **The batch is also an instance of what this entry argues for, and of what it argues is not enough — and the second half is the part worth keeping.** The dating fence flagged five referrers, and the catches split three ways rather than two — which matters, because lumping them credits the weakest control with the strongest catch.

  **The machine forced one on its own**: P083's above-appetite partition count is computed by the bolded-partition invariant from the entries' own cells, so it red-built on "Eleven of the 16" the moment R021's residual moved, with or without anyone reading. **The read pass found two** that no check reaches: R007's treatment-separation argument citing "R021's is Accept", and P083's stale score line. Those are the ones a dating-only pass would have left standing.

  **Two more were inside R021 itself and the read pass did not find them** — the risk scorer did, at the commit gate. So the batch's corrections split by the control that caught them: machine-forced, found by reading, found at the gate. **Deliberately not totalled.** Each gate pass moved the total, so a cardinal here would have gone stale on the pass that read it — three times over, which is the same self-defeating shape as pinning a scenario count. The split is the finding; the tally was never load-bearing. A rationale paragraph still cited "the Likelihood 1 above" against a likelihood the same batch had moved to 2, and the Treatment carried a surviving fragment duplicating the fuller sentence beneath it. Both are shapes this entry already names: a rationale gone stale against the very score being moved, and a surviving fragment of a withdrawn draft. Attributed by catching control rather than totalled, because crediting the read pass with catches it did not make is the error this entry records itself committing three times.

  The sharper finding is WHERE they were. Every widening so far has been about reading radius: phrase, sentence, section, whole file, then inbound referrers. These two sat in the entry being edited, in the paragraphs being edited, and survived. Radius was never the binding constraint on this class — an edit that moves a cardinal has to be checked against every rationale that argued FOR the old value, and those rationales live nearest the change, not furthest from it.

- 2026-08-08: **The review fence cascades across commit boundaries, and it is invisible until CI.** `movedAt()` applies its Change-Log-only discount ONLY when the file is dirty — the check's own note says so — so a fan-out run in the working tree is green by construction, and the verdict that matters only exists once the files commit. Split the fan-out across two commits and the fence re-arms one hop out: the entry moved in commit 1 becomes a stale referrer of the entries moved in commit 2. Fired twice on 2026-08-08 before the mechanism was understood, once in CI on a red trunk.

  Two consequences worth carrying. A local `npm run test:js` on a dirty tree is not evidence about this check; only a clean-tree run is. And a fan-out must land as ONE commit covering the full reference closure, computed to fixpoint rather than one level deep — on 2026-08-08 that closure was **ten of the sixteen active entries**, reached in two iterations from a single-entry change.

  **Key the closure on COMMIT MEMBERSHIP, not on whether a citation still holds.** The fence scores dating. An entry whose outbound citations all survive review still needs a dated bullet when the entries it cites move; excluding it as "already correct" leaves it a stale referrer. That misapplication is what produced the second miss on 2026-08-08.

**An equal cardinal is not confirmation two closures are the same.** The 2026-08-05 fan-out was also ten entries, but converged in one iteration from a different seed; the 2026-08-08 one took two. Reading the matching number as confirmation is this entry's own number-asserted-rather-than-computed shape, and it happened here before the risk gate caught it.

The enumerator this entry says nothing generates does in fact exist: the assertion message names every stale pair. Committing locally and reading the failure is the loop.

- 2026-08-08: Re-verified against R021's same-day change (its Monitoring re-assess trigger fired on run `31252424980`, a push-tier apply that failed by deploying an unpublished version; mechanism fixed, re-rate tracked on P095). **This entry's citation of R021 still holds**: the failure does not change what R021 is about, only its likelihood, and R021 now says of itself at its own surface that its residual understates until the re-rate lands. No cardinal here is affected.
- 2026-08-05: Created and curated at the close of the P083 drain, on the risk scorer's finding that the drain closed the instance and left the class. Landed with the invariants test as its evidenced control.
- 2026-08-05: **Twelfth check landed — the canonical-cell shape, applied to the fact that drifted hardest.** R021's Monitoring `Metrics` line is authoritative for the push-tier apply count and every prose statement of it under `docs/` must agree. It found a real bug in a shared helper on its first run: `WORD_NUM` returned `indexOf`'s `-1` for a non-numeral, which passes an `isInteger` guard, so a loose capture comparing "successful" as a count went unnoticed; now returns `NaN`, which is the safer contract at all four call sites.
  **Its expression took three iterations and each failure is the same error from a different side**: adjacency-only bound to "successful" in `three successful production applies`; allowing intervening words then bound to "is" in "Base rate is three successful…"; dropping the noun anchor matched "two applies instead of one" in P077, which is deferral arithmetic and not this axis. A loose capture in a backtracking engine always finds _some_ word. Anchored at both ends it caught the surviving P083 sibling the scorer had named, which is the control finding the instance rather than me.
- 2026-08-05: **The review-fence check's scope was the defect, twice, and both versions were caught before shipping.** Version one treated any `R<NNN>` substring as an inbound reference and the scorer enumerated ten stale pairs it would have red-built master on. Version two excluded the whole `## Change Log` section — which closed none of those ten, unsaw a live cross-claim, and cited an exemplar that was not in a Change Log at all. Version three exempts only what is structurally quoted, token-locally, which is the same rule the citation check already used.
  The check also now answers at the tier it is invoked at: a dirty target counts as changing _now_ rather than being skipped, so a clean referrer of a moving entry fails at commit time instead of first in CI. That retired an `examined > 0` guard which had passed this batch on the nine entries the batch never touched — a guard asserting the check evaluated _something_, not that it evaluated _this_.
  Two rounds of hand-closure (R003, R006, R008, R009, first- and second-order) were done against the unbounded definition. Those verifications are real and stay; the reason they were needed was the check.
- 2026-08-05: **Two checks landed and the residual came down 8 → 6.** A first draft put it at 4 and within appetite; the scorer withdrew that on the ground that the drop credited both checks with closing the class while one covered a single phrasing and the other did not run in the gate at all — crediting configured coverage by its name, which is the same sub-shape this entry already records itself committing once. The two checks reach two of the four recorded moves of the miss — not the class. Likelihood 4 → 3, residual 8 → 6. **Second instance of a sub-shape this entry already records committing once**: a surviving fragment of a withdrawn draft left inside the Change Log bullet that documents the withdrawal. The earlier version of this sentence kept the draft's "close the classes every observed instance occupied" and "4 → 2", both contradicted by the body two sections above and arithmetically self-refuting against the same bullet's own "8 → 6" at Impact 2. The scorer named both after withdrawing its own recommendation that this batch record an acceptance of the standing 8 — that recommendation had imported this entry's lifetime baseline as the per-action score, which is R027's defect, and an acceptance is in any case what this project's standing direction forbids: it is either within appetite or controls are added to bring it there. Controls were added.
- 2026-08-05: **Fifth consecutive instance, and the first observed on entries curated AFTER this entry's control landed.** Discharging R020's treatment falsified fields across **five** files — R020, R022, R021, R015 and P083. An earlier version of this very entry said "seven fields across R020 and R022", naming only the files the sweep had looked at, and so reproduced the scoping error it was describing. The sites were an H1, a Status line, a Monitoring metric and its spent trigger, a Base-rate section, a stale review date, and a surviving fragment of a withdrawn draft sitting inside the Change Log entry that documented the withdrawal. **All nine structural invariants stayed green throughout**, correctly: not one of the falsified fields was arithmetic. My verification was `grep -c "never exercised"` — a single wording of a defect that turned out to have several — which is the structural-check-over-prose error this entry names, committed while checking for it. R022 carried two falsifications in one sentence-block, forty words apart, the second surviving a sweep that fixed the first — and R015 then repeated that shape exactly, twenty words apart, in the same commit that documented it.

  **The miss moved twice, and the second move is the lesson.** Pass one missed siblings by grepping a phrase. Pass two read whole files and missed siblings _inside_ them. Pass three read whole files and missed **inbound references in files that were never opened** — R021's Siblings clause and R015's, both claims about R020 living outside R020. Whole-file reading closes the intra-file class and does nothing for the cross-file one. The scope rule has to change, not the reading method: before a claim is called swept, enumerate every file carrying an inbound reference to **every entry whose state moved in the batch** — not only the entry that prompted the sweep — and read each of those whole. That widening is the **fourth** move of the miss and was found the same way as the first three: this entry's own residual moved 6 → 8 in the same sitting, its referrers were never enumerated, and the stale `R028 at 6` citation survived in P083, under a partition digit that was still correct. The rule is **partly** mechanised — see the last two checks below, and note what they do not reach. The fence counts uncommitted files as current, so a batch that moves an entry and leaves an inbound referrer stale **passes by construction**: the referrer is only flagged once the move is committed and the referrer is not. That is the same-batch case, and it is how R028's own exemplar above shipped falsified — R010's demotion landed in this batch and the sentence describing R010 did not. Reaching it means comparing against the index rather than the working tree, which is not built.

  Recorded here rather than in the drift table above, deliberately: that table's caption scopes it to one batch and its total is cited as "in the batch", so appending a later instance would leave the invariant green over a sentence it had just made false — this entry's own ceiling, reproduced inside its own control. Likelihood raised 3 → 4 and residual 6 → 8 on the five-for-five base rate.

- 2026-08-05: **Fourth consecutive instance recorded rather than quietly fixed**, per standing direction. Re-scoring this entry from 2 to 6 moved the register's above-appetite partition from ten-of-16 to eleven-of-16, and P083's close-out kept asserting ten — in the exact surface the "What the control does not cover" section had just named as uncovered, in the batch landing this entry's own control. The scorer caught it by recomputing from the files rather than accepting the assertion. Treatment updated in the same pass: the partition check now computes it and asserts any document claiming it agrees, so this specific class is closed and the entry is no longer locally unfixable.
- 2026-08-05: **Residual corrected 2 → 6, from within appetite to above it, before the entry was ever committed.** The draft named free prose as uncovered and then scored as though it were not. The correction has its own evidence: the batch that applied nine remediations for "counts asserted rather than computed" itself shipped a wrong count — P083 asserted 15 active entries in two sentences while the same file's arithmetic, its Progress table and the filesystem all said 16. That is the third consecutive batch in which careful hand-maintenance produced the defect it was correcting, and it is why Likelihood 3 rather than 1.
- 2026-08-05: **The fence flagged ten and corrected none, while two real falsifications sat inside the files it flagged.** Clearing R022's live subject moved R021 and R022; the fence flagged ten referrers and every one was a verification that held. Meanwhile R022's Description, its re-scope section and both Treatment sentences still asserted the drift was live — in the commit whose Change Log recorded it cleared, in the field ADR-059's judgement-fallback filter reads — and R021 and R020 stated the axis base rate as three in seven body sites against their own 3 → 4 bullets. All eleven invariants green, and the fence green **by construction**, because same-batch dirty targets count as current, which this entry's Controls section already predicted.
  The diagnosis is not that the fence is too expensive. It is **pointed at the wrong noun**: it checks file-level reference ordering, and the class that has now fired six consecutive times is sentence-level claim state _inside files the fence already flagged and I had already opened_.
  **The shape is claim-scoped, not locality-scoped — and that is the finding this entry was missing.** Successive sweeps read at a phrase, then a sentence, then a whole file, then a section, and each one missed. The section-level read is decisive because it failed in _both_ directions at once: two surviving sites were **inside** the section being read, and three were the same claim recurring in other sections of the same file. A wider reading radius only helps when the claim happens to be locality-bounded, and these were not. "The axis has fired four times" is a file-global fact living in three sections of R020 and two of R021; "option 1 was taken" is a section-global fact appearing twice in one list item and once in a closing paragraph, with opposite mechanisms.

  **And the claim-scoped method has its own failure mode, demonstrated on the batch that introduced it.** It held for both facts that were enumerated, and missed a third that had also moved — what the two checks accomplished — because nothing generated the enumeration; it was written by hand. Its grep root was `docs/risks/*.active.md` while both docs-wide invariants already walk `docs/`, so the predicted register-scoped miss landed exactly as predicted, in P083. The next unmechanised step is therefore not a wider reading radius but **generating the list of moved facts**, which nothing does.

  So the unit of a sweep is **the claim**: every sentence asserting the same fact, wherever it lives. The method that finally worked was to enumerate each moved fact and grep the whole register for every phrasing of it, rather than to read any region however carefully. The predicted next miss follows directly: a claim scoped to the **register** rather than to a file — the axis base rate now lives in R020, R021 and R028, one fact in three files, and no reading radius reaches that.

  The scope rule recorded above (enumerate every file with an inbound reference to every entry that moved) is the right shape and was executed correctly; it is aimed at files, and a fact is not a file.

  **The dated-bullet rule, stated once because the project decided it both ways in a single batch.** A `## Change Log` bullet that opens with an explicit date is a record of what was true on that date, and keeps its contemporaneous figure — that is the same discipline applied to R025 at retirement, where dating the claims made them permanently true instead of perpetually maintained. A present-tense assertion in a body section is live regardless of what heading it sits under, which is why the review-fence exempts only what is structurally quoted and not a whole region. The test comment already asserted the second half; the first half was applied to R021's dated curation bullet and never written down. Both halves now are. Under it, R021's `- 2026-08-04:` bullet correctly keeps "three", and any undated present-tense statement of the same fact does not.

  Two of these instances were **created by the remediation for the previous batch** — a cardinal updated from three to four in two places without its qualifier, and a hedge spliced onto a sentence whose framing had just been rewritten. That is the seventh consecutive batch to produce the defect it was correcting.

  **The shape as previously named — the corrected sentence's neighbours inside the same section, seven occurrences —** R022 twice before, R022 twice again bracketing the sentence that was fixed, R021 twice and R020 once. Every sweep so far has read at the sentence and not the section. That is the noun a control would have to be aimed at, and none of the candidates below reaches it.

  **Candidate next controls, with coverage measured rather than assumed.** A Monitoring `Metrics` count agreeing with the same entry's Change Log: 1 of 7 on one batch, **0 of 5** on the next — 1 of 12 overall. A cardinal agreeing with the length of the backticked enumeration it introduces: **2 of 5** on the second batch, **0 of 5** on the third. A third candidate, better aimed than either: declare R021's Monitoring `Metrics` line the canonical cell for the axis-apply count, then assert that any `<cardinal> production applies` / `<cardinal> successful applies` / `fired on production <cardinal> times` under `docs/` agrees with it — **2 of 5** on the third batch, and it would also have caught the 3 → 4 drift the batch before spread across R020, R021 and this entry. Same shape as the score-citation check that already works: one canonical cell, many prose citations. None of the three reaches the Treatment-framing sites. **All three** are recorded with their ratios because a control credited for more than it reaches is the error this entry records itself making three times, and none is claimed as a residual reduction. **This sentence is itself the third occurrence of the cardinal-behind-its-enumeration template**: a prior remediation added the third candidate and left "Both"/"neither" untouched — created by the fix for the class it prices at 2-of-5 then 0-of-5, two sentences earlier.

  Superseded framing: assert that a Monitoring `Metrics` count agrees with the highest corresponding count in the same entry's Change Log. Exact and mutation-provable — it would have caught R021's "Three, all successful, as of 2026-08-04". It would have caught **one of the seven** falsifications in this batch; the other six are prose state claims, not counts. Recorded with that ratio because a control credited for more than it reaches is the error this entry records itself making three times.

- 2026-08-05: **First fully-exercised bounded closure.** R021 and R022 moved when R022's live `deploy/**` drift was cleared, the fence flagged six referrers, each was checked rather than touched, and touching those pulled in three more (R009, R015 and this entry) before the set closed. Ten of sixteen entries, one iteration deep, and it terminated — which is what "bounded" means in practice and is more expensive than the word suggests. Recording the shape because the cost is the honest argument for the token-level exemption and against the symmetric widening: both would have made this unbounded.
- 2026-08-05: **The two intra-file moves reached, and two defects found in the checks themselves.** Added a `## Canonical state` declaration to R020 and R022 — a table of fact, value, and the phrasings that would contradict it — and an invariant that enforces it, which mechanises the enumeration this entry says nothing generates. Mutation-proven: restoring R022's Status alone produced `"the half is live" contradicts declared \`deploy/**\` working-tree drift = cleared 2026-08-05 at \`50f1360\``. Then the fence itself was found broken twice. Its dirty-set parse ran `git status --porcelain`output through a whole-output`.trim()`, which strips the leading space of the **first** line, so `slice(3)` ate a character and the first dirty file fell out of the set — on every run since the check landed, silently, and a check that fails open on its first input is precisely the shape this file exists to catch. And the fence counted a Change-Log-only edit as a move, while its own prescribed remedy for a moved target **is** a Change-Log edit, so each remedy re-armed the check one hop further out; R009 accumulated two bullets whose entire content was that a verification bullet elsewhere did not affect it. Fixed asymmetrically — Change-Log-only discounts the **target** axis only, because on the referrer axis that edit is the remedy and must discharge the fence. A first cut applied it to both and left eleven pairs unable to discharge a fence they had already answered. Nine stacked verification bullets across R003, R008, R009 and R027 consolidated into four; restating the same verification three times is the decay this entry is named after. Residual held at 6: reachable-on-declaration is a real step, but the declarations are still remembered rather than generated.
