---
'@mountainpass/addressr-deployment': patch
---

Provision the managed billing webhook through Terraform and pass its signing secret directly to the gateway, removing manual secret copying. Customer access and catalogue sales remain disabled pending launch verification.
