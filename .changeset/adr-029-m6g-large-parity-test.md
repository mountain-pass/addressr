---
'@mountainpass/addressr': patch
---

Resize the v2 OpenSearch parity domain to a larger instance class to measure warmed search latency against v1 (ADR 029). The v2 domain carries no production traffic; this is a sizing measurement, not a cutover.
