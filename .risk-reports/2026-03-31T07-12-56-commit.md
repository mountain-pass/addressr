# Pipeline Risk Report

## Pipeline Summary

| Layer | Scope | Residual Risk |
|-------|-------|--------------|
| Unreleased | Governance framework, agents, hooks, docs, ADRs, skills (all `.claude/`, `docs/`, config -- no runtime code) | 1/25 (Very Low) |
| + Unpushed | Same scope -- 7 unpushed governance commits | 1/25 (Very Low) |
| + Uncommitted | Risk-scorer agent split into 4 mode-specific files + hook deny message updates (10 staged `.claude/` files) | 1/25 (Very Low) |

### Layer 1: Unreleased Changes (release risk)

No `origin/publish` branch exists, so unreleased scope equals the unpushed diff against `origin/master`.

- Scope: 182 files changed across `.claude/`, `docs/`, `governance/`, `scripts/`, and root config. All are governance, documentation, agent definitions, hooks, and developer tooling. The only non-governance files are `package.json` and `package-lock.json` (adding dev tooling deps) and `scripts/` (developer convenience scripts). None of these are in the npm package runtime path.
- **Residual risk: 1/25 (Very Low)**

No runtime, API, or infrastructure code is in the unreleased queue.

### Layer 2: Unreleased + Unpushed (push risk)

- Scope: Same as Layer 1 -- unpushed commits are the entire unreleased queue.
- **Cumulative residual risk: 1/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

- Scope: 10 staged files, all under `.claude/`:
  - `risk-scorer.md` refactored -- bulk content moved to 4 new mode-specific agents (`risk-scorer-pipeline.md`, `risk-scorer-plan.md`, `risk-scorer-policy.md`, `risk-scorer-wip.md`)
  - 4 hook scripts updated with improved deny messages (`git-push-gate.sh`, `risk-score-commit-gate.sh`, `risk-score-plan-enforce.sh`, `wip-risk-gate.sh`)
  - 1 skill file path reference updated (`risk-policy/SKILL.md`)

#### Risk 1: Governance config change breaks hook routing

- Inherent impact: 1/5 (Negligible) -- `.claude/` files are developer tooling config; a broken agent reference would cause a hook to fail to invoke an agent, not affect any user-facing service
- Inherent likelihood: 2/5 (Unlikely) -- the refactor splits one file into four with updated references; routing errors are possible but straightforward
- Inherent risk: 2/25 (Very Low)
- Controls: None needed -- impact is Negligible
- Residual impact: 1/5 (Negligible)
- Residual likelihood: 2/5 (Unlikely)
- **Residual risk: 2/25 (Very Low)**

- **Cumulative residual risk: 1/25 (Very Low)** (Layer 1 base of 1 + this commit adds negligible incremental risk; cumulative stays at 1 since the commit is risk-neutral to the release)

### Bypass

All 10 staged files are `.claude/` governance configuration. These files are not included in the npm package, Docker image, or runtime path. The commit is **risk-neutral** to cumulative release risk. Bypass marker written.

## Pipeline Flow

- Pipeline WIP is entirely governance/tooling -- no runtime risk is accumulating.
- Commit and push freely. No back-pressure from downstream queues.
- When runtime changes begin, a changeset and release cycle will be needed for the dependency updates in the unreleased queue.
