## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

- Scope: Recent commits are all `chore:` changes to developer tooling (risk-scorer refactoring, lifecycle tests, deterministic score writing, retrospective updates). No runtime, API, or package code changes.
- Risks: None material. All changes are to developer governance tooling, not to the addressr service itself.
- **Residual risk: 1/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

- Scope: No unpushed commits beyond what is already unreleased.
- Additional risks from unpushed changes: None.
- **Cumulative residual risk: 1/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

- Scope: Staged change is `package-lock.json` only -- adding the `bats@1.13.0` entry to align the lockfile with the existing `package.json` devDependency declaration. Also `docs/BRIEFING.md` modified (documentation only).

#### Risk 1: Lockfile sync for bats devDependency
- Inherent impact: 1/5 (Negligible) -- devDependency lockfile update; no runtime, publish, or API path affected
- Inherent likelihood: 1/5 (Rare) -- `package-lock.json` is generated deterministically by npm; adding a missing entry is well-understood
- Inherent risk: 1/25 (Very Low)
- Controls:
  - `npm ci` itself validates lockfile integrity -- if the entry is malformed, CI fails immediately before any downstream effect
- **Residual risk: 1/25 (Very Low)**

This change is **risk-reducing**: it fixes a broken CI pipeline where `npm ci` fails due to a missing lockfile entry. The uncommitted diff restores CI to a working state.

- Additional risks from uncommitted changes: None. The `docs/BRIEFING.md` change is documentation only.
- **Cumulative residual risk: 1/25 (Very Low)**

### Pipeline Summary

| Layer | Scope | Residual Risk |
|-------|-------|--------------|
| Unreleased | Chore commits: tooling, governance | 1/25 (Very Low) |
| + Unpushed | No additional | 1/25 (Very Low) |
| + Uncommitted | package-lock.json sync + BRIEFING.md docs | 1/25 (Very Low) |

No back-pressure warnings. All layers are well below the risk appetite threshold of 5.

RISK_SCORES: commit=1 push=1 release=1
RISK_BYPASS: reducing
