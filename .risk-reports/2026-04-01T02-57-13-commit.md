Now I have all the context needed. Let me also check the unreleased/unpushed state based on the git status provided.

The git status shows:
- **Uncommitted (staged)**: `docs/BRIEFING.md` (modified), `.risk-reports/` (untracked)
- **Unpushed**: Recent commits (5dff8bc through 7b82814) -- these are chore/infrastructure commits
- The user is asking about staged changes to `package.json` and `package-lock.json`, but the git status snapshot shows only `docs/BRIEFING.md` as modified. The user describes what they intend to stage/commit. I'll assess based on the described changes.

## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

The unreleased queue includes recent chore commits (deterministic score writing, lifecycle tests, risk-scorer split, gate file path redaction, retrospective/BRIEFING update). These are all developer tooling and process infrastructure -- no runtime code changes.

- Scope: Hook scripts, risk scoring infrastructure, BRIEFING.md updates
- Risks: None material -- these are developer tooling changes that do not affect the npm package, Docker image, or live API
- **Residual risk: 1/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

Same as Layer 1 -- the unpushed commits are the unreleased commits.

- Scope: Same as above
- Additional risks from unpushed changes: None beyond Layer 1
- **Cumulative residual risk: 1/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

Adding the proposed commit: removing `ngrok` devDependency from `package.json` and regenerating `package-lock.json`, plus `docs/BRIEFING.md` update.

#### Risk 1: Removing ngrok devDependency breaks something that uses it
- Inherent impact: 2/5 (Minor) - ngrok is a devDependency only; removing it cannot affect the published npm package, Docker image, or live service
- Inherent likelihood: 1/5 (Rare) - The only reference in the codebase is a commented-out block at lines 136-139 of `/Users/tomhoward/Projects/addressr/swagger.js`. No active import or require exists.
- Inherent risk: 2/25 (Very Low)
- Controls:
  - Grep of codebase confirms ngrok is only referenced in commented-out code in `swagger.js` -- reduces likelihood to remain at 1
- **Residual risk: 2/25 (Very Low)**

#### Risk 2: Regenerated package-lock.json introduces unexpected dependency changes
- Inherent impact: 2/5 (Minor) - lock file changes could alter transitive dependency versions
- Inherent likelihood: 1/5 (Rare) - removing a single devDependency from the lock file is a subtraction, not an addition. npm regeneration of the lock file for a removal is deterministic.
- Inherent risk: 2/25 (Very Low)
- Controls:
  - CI test suite (`test:nogeo`, `test:geo`) exercises the full runtime path and would catch dependency regressions
- **Residual risk: 2/25 (Very Low)**

This change is **risk-reducing**: it removes a broken devDependency whose postinstall downloads from `bin.equinox.io` (returning HTTP 500), which blocks CI. The net effect is fixing the CI pipeline.

- Scope: devDependency removal + lock file regeneration + BRIEFING.md docs
- Additional risks from uncommitted changes: Negligible
- **Cumulative residual risk: 1/25 (Very Low)**

### Pipeline Summary

| Layer | Scope | Residual Risk |
|-------|-------|--------------|
| Unreleased | Chore commits: hook scripts, risk scoring infra, BRIEFING.md | 1/25 (Very Low) |
| + Unpushed | Same as unreleased | 1/25 (Very Low) |
| + Uncommitted | Remove ngrok devDep, regen lock file, BRIEFING.md | 1/25 (Very Low) |

No downstream back-pressure concerns. No confidential information disclosure detected.

RISK_SCORES: commit=1 push=1 release=1
RISK_BYPASS: reducing
