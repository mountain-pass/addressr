# Pipeline Risk Report — Commit Assessment

**Date:** 2026-03-31T07:03:03Z
**Staged change:** `.claude/agents/risk-scorer.md` — security hardening constraint (hide gate file paths from report output)
**Policy last reviewed:** 2026-03-30 (current)

## Layer 1: Unreleased Changes (release risk)

No `origin/publish` branch exists, so there is no separate unreleased queue to assess. All unpushed commits would become the first release if pushed and released.

**Residual risk: 1/25 (Very Low)** — nothing is currently released or queued for release.

## Layer 2: Unreleased + Unpushed (push risk)

The 6 unpushed commits contain:

- **Major version changeset** declaring `@mountainpass/addressr: major` with Node.js >=22 requirement, opensearch v2->v3, and 15+ major dependency upgrades
- Governance framework: 60+ agent definitions, 30+ hooks, skills, settings
- Architecture docs, ADRs, decision records, problem tickets
- `bats` dev dependency added, new npm scripts (`push:watch`, `release:watch`, `test:hooks`)
- `package.json` and `package-lock.json` changes

### Risk 1: Major dependency upgrades break runtime behaviour

- Inherent impact: 4/5 (Significant) — opensearch v3, keyv v5, glob v13, json-ptr v3, and dotenv v17 are all runtime dependencies. Breaking changes could cause incorrect address results, search failures, or startup crashes for npm/Docker consumers.
- Inherent likelihood: 4/5 (Likely) — 15+ major version bumps across runtime dependencies is high-complexity with wide-reaching behaviour changes.
- Inherent risk: 16/25 (High)
- Controls:
  - CI pipeline (`.github/workflows/`) — will-execute on push — runs full test suite against opensearch service container. Reduces likelihood from 4 to 3 if tests cover the changed API surfaces. However, cannot name specific tests that exercise opensearch v3 query API, keyv v5 caching, or glob v13 file resolution without inspecting test files.
  - Changeset declares `major` — correctly signals breaking change to consumers, reducing surprise. Does not reduce likelihood of defect, but reduces impact from 4 to 4 (consumers are warned).
- Residual impact: 4/5 (Significant)
- Residual likelihood: 3/5 (Possible) — CI tests provide some coverage but cannot confirm specific failure scenarios are tested
- **Residual risk: 12/25 (High)**

### Risk 2: Node.js >=22 requirement breaks existing consumers

- Inherent impact: 4/5 (Significant) — consumers on Node 14-20 will get post-install failures
- Inherent likelihood: 2/5 (Unlikely) — the `major` version bump signals this correctly; consumers using semver ranges will not auto-upgrade
- Inherent risk: 8/25 (Medium)
- Controls:
  - Major version changeset — will-execute — consumers on `^current` won't auto-upgrade
- Residual impact: 4/5 (Significant)
- Residual likelihood: 1/5 (Rare)
- **Residual risk: 4/25 (Low)**

### Risk 3: Dev dependency and scripts changes

- Inherent impact: 2/5 (Minor) — `bats` is devDependency only, scripts are developer tooling
- Inherent likelihood: 1/5 (Rare) — straightforward additions
- Inherent risk: 2/25 (Very Low)
- **Residual risk: 2/25 (Very Low)**

**Overall Layer 2 — driven by Risk 1:**

**Cumulative residual risk: 8/25 (Medium)** — dominated by the major dependency upgrade risk. The governance files are risk-neutral (not in npm package).

## Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

The staged change adds two paragraphs to `.claude/agents/risk-scorer.md` instructing the risk-scorer agent not to expose gate file paths in report output. This is:

- A `.claude/` configuration file — not in the npm package, Docker image, or runtime path
- Security hardening of the governance framework
- **Risk-neutral** to the release

No additional risks introduced by this commit.

**Cumulative residual risk: 8/25 (Medium)** — unchanged from Layer 2. The commit itself adds zero risk.

**Bypass: commit bypass marker created** — this change is risk-neutral (`.claude/` config file not in any runtime or publish path).

## Pipeline Summary

| Layer | Scope | Residual Risk |
|-------|-------|---------------|
| Unreleased | No origin/publish branch | 1/25 (Very Low) |
| + Unpushed | Major dep upgrades, governance framework, docs | 8/25 (Medium) |
| + Uncommitted | risk-scorer.md security hardening | 8/25 (Medium) |

## Suggested Actions

- **Push risk is 8/25 (Medium), above appetite of 5.** The major dependency changeset (opensearch v3, Node >=22, 15+ major bumps) is driving this. Before releasing:
  - Verify CI test suite passes on push — particularly tests exercising opensearch queries, address search, and API responses
  - Consider whether the governance-only commits could be pushed separately from the dependency changeset (the changeset was already on the branch before governance work)
  - Write or verify integration tests specifically covering opensearch v3 query API changes

## Pipeline Flow

- This commit is safe to proceed (risk-neutral, bypass marker written).
- After committing, push to get CI feedback on the accumulated changes. The major dependency upgrades need CI validation before release.
- The release queue will need careful attention — the major version bump with 15+ dependency upgrades warrants thorough CI green + preview verification before merging any release PR.
