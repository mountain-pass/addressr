# Pipeline Risk Report — 2026-03-31T05:14:01Z

## Layer 1: Unreleased Changes (release risk)

**Scope:** Major version bump (v1.1.3 to v2.0.0). 220 files changed, 46,115 insertions, 36,580 deletions. Node.js >=22 (from >=14), opensearch v3, waycharter v2, got replaced with native fetch, 20+ dependency upgrades across major versions, cucumber v5 to v11, eslint v7 to v9, prettier v2 to v3 reformatting.

### Risk 1: OpenSearch v3 client behavioural incompatibility

The opensearch client was upgraded from v2 to v3. Query construction, response shapes, or connection handling may differ in ways not covered by the existing test suite exercising the v2 API contract.

- Inherent impact: 4/5 (Significant) — incorrect or missing address search results for RapidAPI consumers
- Inherent likelihood: 3/5 (Possible) — major version upgrade of a core data layer dependency
- Inherent risk: 12/25 (High)
- Controls:
  - CI OpenSearch service container runs OpenSearch 1.3 matching production — will-execute — confirms client-server compatibility for this specific version. Reduces likelihood from 3 to 2 because the CI environment matches production OpenSearch version, so query/response shape issues would surface during test runs.
  - test/js/steps.js address-search scenarios exercise opensearch query and response paths end-to-end — will-execute — reduces likelihood because these are integration tests against a real OpenSearch instance in CI, not mocked.
- Residual impact: 4/5 (Significant)
- Residual likelihood: 2/5 (Unlikely)
- **Residual risk: 8/25 (Medium)**

### Risk 2: Node.js >=22 engine requirement breaks existing consumers

Consumers on Node 14-20 will fail to install or run. This is a documented breaking change in the major version bump, but consumers who auto-upgrade without reading changelogs will be affected.

- Inherent impact: 4/5 (Significant) — npm package fails for subset of consumers
- Inherent likelihood: 3/5 (Possible) — consumers may not read breaking change notes
- Inherent risk: 12/25 (High)
- Controls:
  - Major semver version bump (v2.0.0) — will-execute — signals breaking change to package managers. Reduces impact from 4 to 3 because consumers with pinned ranges or semver-aware lockfiles will not auto-upgrade.
  - Changeset documents the breaking change explicitly — will-execute — provides visibility. Reduces likelihood from 3 to 2.
- Residual impact: 3/5 (Moderate)
- Residual likelihood: 2/5 (Unlikely)
- **Residual risk: 6/25 (Medium)**

### Risk 3: Waycharter v2 API contract changes

Waycharter upgraded from v1 to v2. The REST API routing and hypermedia link generation flow through waycharter. Changes to its API could alter response link headers or routing behaviour.

- Inherent impact: 4/5 (Significant) — API response structure changes could break consumer integrations
- Inherent likelihood: 3/5 (Possible) — major version of a routing/hypermedia library
- Inherent risk: 12/25 (High)
- Controls:
  - test/js/steps.js REST driver scenarios exercise link-template headers and API response structure — will-execute — reduces likelihood from 3 to 2 because these integration tests verify the actual HTTP responses including headers.
- Residual impact: 4/5 (Significant)
- Residual likelihood: 2/5 (Unlikely)
- **Residual risk: 8/25 (Medium)**

### Risk 4: got-to-fetch migration regression

Replacing got with native fetch changes HTTP client behaviour (redirect handling, timeout semantics, error shapes, stream handling).

- Inherent impact: 4/5 (Significant) — upstream data fetching or API calls could fail silently or behave differently
- Inherent likelihood: 2/5 (Unlikely) — fetch is well-understood and the change scope is limited
- Inherent risk: 8/25 (Medium)
- Controls:
  - test/js/steps.js integration scenarios exercise the full request/response cycle — will-execute — reduces likelihood from 2 to 1 because the tests exercise the actual HTTP paths in CI.
- Residual impact: 4/5 (Significant)
- Residual likelihood: 1/5 (Rare)
- **Residual risk: 4/25 (Low)**

### Overall Layer 1

Highest residual risk item: 8/25 (OpenSearch v3 and Waycharter v2, tied).

- **Residual risk: 8/25 (Medium)**

---

## Layer 2: Unreleased + Unpushed (push risk)

**Scope:** Layer 1 scope plus 4 unpushed commits (173 files, 30,247 insertions). ALL unpushed changes are governance infrastructure: `.claude/` agents, hooks, skills, settings; `docs/` ADRs, architecture docs, C4 model; `scripts/` for push-and-watch and release-watch; `CLAUDE.md` updates; `package.json` dev script additions.

**Additional risks from unpushed changes:** None. All unpushed files are outside the npm package, Docker image, runtime source, and CI workflow paths. They are governance/developer tooling that cannot affect the production service.

- No new risk items beyond Layer 1.
- **Cumulative residual risk: 8/25 (Medium)**

**Bypass:** Push is risk-neutral. Bypass marker written.

---

## Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

**Scope:** Layer 2 scope plus 16 staged files. ALL uncommitted changes are in `.claude/` (agents, hooks, settings), `docs/` (BRIEFING.md, ADR, problem tickets), and `CLAUDE.md`. None are in the npm package, runtime source, CI workflows, or any path that affects the build/publish/deploy pipeline.

**Additional risks from uncommitted changes:** None. These files are:
- `.claude/agents/risk-scorer.md` — agent instructions (not runtime)
- `.claude/hooks/*.sh` — local development hooks (not CI or production)
- `.claude/settings.json` — local Claude Code config
- `CLAUDE.md` — project instructions for Claude Code
- `docs/BRIEFING.md` — developer briefing document
- `docs/decisions/019-*.md` — ADR (documentation)
- `docs/problems/008-011*.md` — problem tickets (documentation)

- No new risk items beyond Layer 2.
- **Cumulative residual risk: 8/25 (Medium)**

**Bypass:** Commit is risk-neutral. Bypass marker written.

---

## Pipeline Summary

| Layer | Scope | Residual Risk |
|-------|-------|--------------|
| Unreleased | Major v2.0.0: Node >=22, opensearch v3, waycharter v2, got->fetch, 20+ dep upgrades | 8/25 (Medium) |
| + Unpushed | Governance: agents, hooks, ADRs, C4 model, scripts (risk-neutral) | 8/25 (Medium) |
| + Uncommitted | Governance: cumulative scoring, bypass markers, briefing system, problem tickets (risk-neutral) | 8/25 (Medium) |

---

## Suggested Actions

- **Layer 1 drives all risk.** The unreleased v2.0.0 queue is the sole source of pipeline risk at 8/25 (Medium, above appetite of 5).
- Write targeted integration tests for opensearch v3 query API edge cases (empty results, pagination, special characters) to reduce Risk 1 likelihood further.
- Write integration tests for waycharter v2 link-template generation to reduce Risk 3 likelihood.
- Consider a release preview deployment with smoke tests against the full address search flow before merging the release PR.
- The commit and push are risk-neutral and should proceed freely (bypass markers written).

---

## Pipeline Flow

- Commit and push the current governance changes freely — they are risk-neutral and bypass markers are in place.
- The unreleased v2.0.0 queue at 8/25 is above appetite. Before releasing, focus on risk-reducing actions: targeted tests for opensearch v3 and waycharter v2, and a verified preview deployment with smoke tests.
- Stale files: none detected.
