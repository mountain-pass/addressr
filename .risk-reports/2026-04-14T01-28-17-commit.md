Now I have enough to assess.

## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

The most recent release was v2.0.0 (commit `b08ad26`). The unreleased queue includes documentation changes (ADR 021, README restructure) and a changeset for patch release (dep upgrades + waychaser fix). These are already on master.

- Scope: Documentation, changeset metadata, dependency upgrades
- Risks: Minimal -- docs and dependency metadata only

- **Residual risk: 1/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

No unpushed commits beyond what is already on master (the staged changes have not been committed yet).

- Scope: Same as Layer 1
- Additional risks from unpushed changes: None
- **Cumulative residual risk: 1/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

The uncommitted staged changes refactor 5 files across the server entry points, data loader, and API controllers.

#### Risk 1: Async/await conversion in server.js and src/server2.js alters startup error propagation

- Inherent impact: 4/5 (Significant) - Server failing to start would mean no API service
- Inherent likelihood: 1/5 (Rare) - The conversion is mechanical: nested `.then()` with `await` inside becomes a flat `.then(async () => { await ... })` with `.catch()`. The semantics are preserved. The `esConnect()` call is awaited, and the `.catch()` on the outer chain catches both sync and async errors.
- Inherent risk: 4/25 (Low)
- Controls:
  - CI `build-and-test` job in `.github/workflows/release.yml` (lines 28-92) -- starts the server with OpenSearch, runs Cucumber feature tests (`npm run test:nogeo`) that exercise Root API, address search, and address detail endpoints. Would catch server startup failures and broken API responses.
  - ESLint promise rules -- the 19 `promise/*` warnings being fixed are themselves a signal; the linter validates correct promise patterns post-change.
- **Residual risk: 2/25 (Very Low)**

#### Risk 2: Added return statements in loader.js .then() callbacks

- Inherent impact: 3/5 (Moderate) - Data loader is used for G-NAF indexing; broken loader means no new data loads, but existing service continues
- Inherent likelihood: 1/5 (Rare) - Adding `return` to `.then()` callbacks (e.g., `return logger(...)`) is the correct fix for `promise/always-return`. The return value of `debug()` is unused by downstream `.then()` handlers; this is strictly a lint compliance fix with no behavioral change.
- Inherent risk: 3/25 (Low)
- Controls:
  - CI test suite exercises the full load-then-search path via Cucumber scenarios ("Given an address database is loaded from gnaf")
- **Residual risk: 1/25 (Very Low)**

#### Risk 3: Added .catch() and return statements in controllers/Addresses.js and controllers/Default.js

- Inherent impact: 4/5 (Significant) - These are the live API endpoints; broken error handling could cause unhandled rejections or silent failures
- Inherent likelihood: 1/5 (Rare) - Reviewing the actual code: `Addresses.js` already had `.catch()` handlers; the change adds `return` to `.then()` callbacks (lines 17, 21, 40, 48). `Default.js` similarly adds `return` to `writeJson()` call (line 11). These are cosmetic -- the return values are not consumed by Express. The `.catch()` handlers were already present.
- Inherent risk: 4/25 (Low)
- Controls:
  - Cucumber feature tests in `test/resources/features/addresses.feature` -- "Searching Address List", "Getting Address - No Index" (503), "Searching Addressed - Place" (404), and pagination scenarios all exercise `getAddress` and `getAddresses` handlers directly through the API.
  - `test/resources/features/root.feature` exercises the `getApiRoot` handler in `Default.js`.
- **Residual risk: 2/25 (Very Low)**

#### Risk 4: Node.js >= 22 requirement

- Inherent impact: 3/5 (Moderate) - If CI or deployments use older Node, build/tests would fail
- Inherent likelihood: 1/5 (Rare) - CI already uses `node-version: '22.x'` (release.yml line 48). The async/await syntax used is basic and has been supported since Node 8. No Node 22-specific APIs are used in the diff.
- Inherent risk: 3/25 (Low)
- Controls:
  - CI enforces Node 22 already
- **Residual risk: 1/25 (Very Low)**

**Combined Layer 3 residual: 2/25 (Very Low)** -- taking the highest individual residual risk.

### Pipeline Summary

| Layer         | Scope                                                                            | Residual Risk   |
| ------------- | -------------------------------------------------------------------------------- | --------------- |
| Unreleased    | Docs, changeset, dep upgrades                                                    | 1/25 (Very Low) |
| + Unpushed    | No additional changes                                                            | 1/25 (Very Low) |
| + Uncommitted | Promise lint fixes: async/await refactor + return/catch additions across 5 files | 2/25 (Very Low) |

### Assessment

This is a **risk-reducing** change. The additions of `.catch()` handlers and `return` statements fix 19 lint warnings and improve error handling robustness. The async/await conversion in the server entry points is semantically equivalent to the prior nested-then pattern. All changes are exercised by the existing Cucumber test suite in CI.

No confidential information detected. No back-pressure warnings needed (all scores well below threshold of 5).

RISK_SCORES: commit=2 push=1 release=1
RISK_BYPASS: reducing
