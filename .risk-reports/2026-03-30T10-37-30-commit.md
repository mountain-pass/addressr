## Commit Risk Report

### Scope of Change
- 24 staged files, all governance/tooling: `.claude/` hooks and agents, `docs/` decision records and problem tickets, `scripts/` helper scripts, `RISK-POLICY.md`, `.changeset/` metadata, `package.json` (npm scripts + bats devDep), `package-lock.json`
- No runtime source code changes. No changes to address search, API, OpenSearch integration, or any production code path.
- The only runtime-adjacent change is `package.json` adding convenience npm scripts (`push:watch`, `release:watch`, `test:hooks`) and `bats` as a devDependency.

### Risks

#### Risk 1: DevDependency or script addition causes build/test regression
- Inherent impact: 2/5 (Minor) - Adding a devDependency and npm scripts affects developer tooling only. The npm package, Docker image, and live service are unaffected. Per RISK-POLICY.md: "No end-user impact; only developer experience or build tooling affected."
- Inherent likelihood: 1/5 (Rare) - Adding a devDependency (`bats`) and three npm scripts is trivial and well-understood. `npm ci` might fail only if the lockfile is inconsistent, but lockfile is included.
- Inherent risk: 2/25 (Very Low)
- Controls:
  - CI build-and-test job (will-execute on push) - reduces likelihood: if `npm ci` fails due to lockfile inconsistency, CI catches it before release. This directly exercises the failure scenario of a broken install.
- Residual impact: 2/5 (Minor)
- Residual likelihood: 1/5 (Rare)
- **Residual risk: 2/25 (Very Low)**

#### Risk 2: Changeset metadata bump to major is premature or incorrect
- Inherent impact: 1/5 (Negligible) - The changeset `.md` file is governance metadata. Changing the bump level from patch to major does not affect any runtime path. If incorrect, it can be amended before release.
- Inherent likelihood: 1/5 (Rare) - The major bump is clearly warranted given the Node.js >=22 breaking change already in the unreleased queue.
- Inherent risk: 1/25 (Very Low)
- Controls: None needed at this risk level.
- Residual impact: 1/5 (Negligible)
- Residual likelihood: 1/5 (Rare)
- **Residual risk: 1/25 (Very Low)**

### Overall Risk
- Overall inherent risk: 2/25 (Very Low)
- Overall residual risk: 2/25 (Very Low)

### Downstream Impact
- Projected push risk: Very Low. Adding these governance files to the existing unpushed commit (also governance) does not meaningfully change the push risk profile. The combined push would be ~164 files, all governance/tooling.
- Back-pressure: None. The commit is governance-only and does not increase downstream risk.
