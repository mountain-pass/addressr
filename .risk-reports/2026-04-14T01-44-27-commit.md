I now have sufficient context. Let me check the `url.parse` to `new URL()` behavioral difference more carefully, since that's the only runtime change.

The key behavioral difference: `url.parse()` returns `path` (pathname + search), while `new URL()` has separate `pathname` and `search` properties. The code now uses `uri.pathname` where it previously used `uri.path`. In `stream-down.js`, `path` was only used for `pathUtil.basename()` to extract a filename and for the console log message. Since `basename` on a path with query string would include the query string in the filename, using `pathname` is actually more correct. The `uri.toString()` vs the original URL string passed to `http.get()` should be functionally equivalent for well-formed URLs.

## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

The unreleased queue contains the recent merge of changeset release (v2.0.0 patch), plus documentation changes. These are already pushed. No additional unreleased risk from this commit's content at the release layer yet.

- Scope: Documentation (ADR 021, README restructure), changeset release
- Risks: Negligible -- docs and version bump only
- **Residual risk: 1/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

No unpushed commits beyond what is already released/on remote. This layer equals Layer 1.

- Scope: Same as Layer 1
- Additional risks from unpushed changes: None
- **Cumulative residual risk: 1/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

- Scope: 7 files staged -- ESLint config rename, eslint-disable comments on false positives, and `url.parse()` to `new URL()` migration in `stream-down.js`

#### Risk 1: ESLint config and lint-disable comments (6 files)

- Inherent impact: 2/5 (Minor) -- ESLint config and inline disable comments affect developer tooling only, not runtime behavior
- Inherent likelihood: 1/5 (Rare) -- Renaming an import variable and adding eslint-disable comments are syntactically trivial
- Inherent risk: 2/25 (Very Low)
- Controls:
  - ESLint itself validates the config on next lint run -- would catch syntax errors in config
  - CI build runs ESLint -- catches any misconfigured rules before merge
- **Residual risk: 1/25 (Very Low)**

#### Risk 2: `url.parse()` to `new URL()` migration in `stream-down.js`

- Inherent impact: 3/5 (Moderate) -- `stream-down.js` is used to download the G-NAF data file; a failure here would prevent new G-NAF index builds but would not affect running services
- Inherent likelihood: 1/5 (Rare) -- `new URL()` is the standard replacement for deprecated `url.parse()`; `uri.pathname` is more correct than `uri.path` for basename extraction (excludes query string); `uri.toString()` is equivalent to the original URL for `http.get()`
- Inherent risk: 3/25 (Low)
- Controls:
  - `n/no-deprecated-api` ESLint rule -- confirms `url.parse` was deprecated and `new URL()` is the correct replacement
  - G-NAF download is exercised in CI integration tests (the cached G-NAF fixture path validates the download mechanism is invoked correctly)
- **Residual risk: 2/25 (Very Low)**

#### Confidential Information Disclosure Check

No business metrics, revenue figures, user counts, pricing, or traffic volumes found in any staged change. PASS.

### Pipeline Summary

| Layer         | Scope                                | Residual Risk   |
| ------------- | ------------------------------------ | --------------- |
| Unreleased    | Docs, changeset release              | 1/25 (Very Low) |
| + Unpushed    | No additional                        | 1/25 (Very Low) |
| + Uncommitted | ESLint cleanup + url.parse migration | 2/25 (Very Low) |

This commit is risk-reducing: it replaces a deprecated Node.js API (`url.parse`) with the modern standard (`new URL`), eliminates all non-size ESLint warnings (improving code quality signal), and adds precise eslint-disable comments for documented false positives rather than suppressing rules globally.

RISK_SCORES: commit=2 push=1 release=1
RISK_BYPASS: reducing
