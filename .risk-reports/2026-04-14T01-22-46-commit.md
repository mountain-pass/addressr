## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

The unreleased queue contains the recent release prep (changeset, ADR 021, README restructure) plus these lint fixes. All are non-functional.

- Scope: Documentation, changeset, and lint compliance fixes
- Risks: Negligible -- no runtime behavior changes
- **Residual risk: 1/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

- Scope: Same as Layer 1 (recent commits are already pushed per git status showing no unpushed commits beyond staged changes)
- Additional risks from unpushed changes: None beyond what is uncommitted
- **Cumulative residual risk: 1/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

- Scope: 6 files -- ESLint config overrides, trivial code fixes (unused var, undefined refs, empty catch comment, eslint-disable), and a new lint regression test

#### Risk 1: ESLint rule suppressions hiding real issues

- Inherent impact: 2/5 (Minor) - suppressed rules could mask a real missing dependency
- Inherent likelihood: 1/5 (Rare) - suppressions are scoped to specific directories (ci/**, test/k6/**, deploy/\*\*) where deps are resolved by external runtimes (Dagger, k6, deploy context), not Node.js
- Inherent risk: 2/25 (Very Low)
- Controls:
  - `test/lint/all-rules.test.js` -- new test asserts zero ESLint errors across the entire codebase, catching any future regressions
  - Existing CI test suite exercises actual imports at runtime, so truly missing deps would fail integration tests
- **Residual risk: 1/25 (Very Low)**

#### Risk 2: Behavioral change in ci/build.js (const runner = ... -> await ...)

- Inherent impact: 2/5 (Minor) - CI pipeline build step
- Inherent likelihood: 1/5 (Rare) - removing an unused variable assignment while keeping the awaited call is semantically identical
- Inherent risk: 2/25 (Very Low)
- Controls:
  - CI pipeline itself exercises this code path on every build
- **Residual risk: 1/25 (Very Low)**

#### Risk 3: Removed console.log calls in ci/pipeline.mjs

- Inherent impact: 1/5 (Negligible) - logging removal in CI, and the variables were undefined anyway (no-undef errors)
- Inherent likelihood: 1/5 (Rare) - removing references to undefined variables cannot change behavior
- Inherent risk: 1/25 (Very Low)
- **Residual risk: 1/25 (Very Low)**

### Pipeline Summary

| Layer         | Scope                                        | Residual Risk   |
| ------------- | -------------------------------------------- | --------------- |
| Unreleased    | Docs, changeset, lint fixes                  | 1/25 (Very Low) |
| + Unpushed    | No additional                                | 1/25 (Very Low) |
| + Uncommitted | ESLint config, trivial code fixes, lint test | 1/25 (Very Low) |

All changes are lint compliance fixes with no runtime behavioral changes. The new `test/lint/all-rules.test.js` is risk-reducing as it prevents future lint regressions.

RISK_SCORES: commit=1 push=1 release=1
RISK_BYPASS: reducing
