Now I have a clear picture. Let me assess the pipeline risk.

## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

Based on the git status, the most recent release was `b08ad26 chore: release` and there are subsequent commits (`5e0fd2d`, `98c6552`, `3c86be8`) plus whatever is about to be committed. The unreleased queue contains documentation changes (ADR 021, README restructure) and a changeset for patch release. These are documentation-only and pose negligible release risk.

- Scope: Documentation updates (ADR, README), changeset metadata
- Risks: None material -- documentation does not affect runtime
- **Residual risk: 1/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

No unpushed commits beyond the unreleased queue (branch is master, recent commits appear pushed based on the merge commit at HEAD).

- Scope: Same as Layer 1
- Additional risks from unpushed changes: None
- **Cumulative residual risk: 1/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

The staged changes comprise:

1. **File renames** (3 files: `printVersion.js` -> `print-version.js`, `setLinkOptions.js` -> `set-link-options.js`, `waycharterServer.js` -> `waycharter-server.js`)
2. **Import path updates** across 5 consuming files to match the renames
3. **ESLint config tightening** (filename-case warn->error, removed overrides for rules that now use recommended error level)
4. **One `null` -> `undefined` change** in `create-deployment-archive.js` (`JSON.stringify` replacer argument)
5. **One eslint-disable comment** added to `loader.js` for legitimate `process.exit()`

#### Risk 1: Broken import paths after file renames

- Inherent impact: 4/5 (Significant) - If any import is missed, the server or loader would fail to start
- Inherent likelihood: 2/5 (Unlikely) - Renames are mechanical and all consuming files have been updated; I verified `loader.js`, `server.js`, `src/server2.js`, `service/DefaultService.js`, `service/address-service.js`, and `test/js/world.js` all reference the new paths correctly
- Inherent risk: 8/25 (Medium)
- Controls:
  - Cucumber/integration test suite (`test/js/world.js` imports `waycharter-server` and exercises the server start path) - reduces likelihood from 2 to 1 because the test suite will fail to import if the path is wrong
  - ESLint `import-x` plugin (configured in `eslint.config.js` lines 30-31) - catches unresolved imports at lint time, reduces likelihood from 2 to 1
  - Node.js module resolution - application will fail immediately at startup with a clear error if any import is wrong
- **Residual risk: 4/25 (Low)**

#### Risk 2: JSON.stringify behaviour change (null -> undefined)

- Inherent impact: 3/5 (Moderate) - This is in `create-deployment-archive.js` which generates the AWS deployment package.json
- Inherent likelihood: 1/5 (Rare) - `JSON.stringify(obj, null, 2)` and `JSON.stringify(obj, undefined, 2)` are semantically identical per the ECMAScript specification; both mean "no replacer function"
- Inherent risk: 3/25 (Low)
- Controls:
  - ECMAScript specification guarantees equivalence - no functional change
- **Residual risk: 1/25 (Very Low)**

#### Risk 3: ESLint config changes causing CI failures

- Inherent impact: 2/5 (Minor) - Only affects developer experience / CI, not runtime
- Inherent likelihood: 2/5 (Unlikely) - The renames and code changes in this commit are specifically to comply with the tightened rules; any remaining violations would be caught by the lint step itself before merge
- Inherent risk: 4/25 (Low)
- Controls:
  - CI lint step will run with the new config and catch any remaining violations before merge
- **Residual risk: 2/25 (Very Low)**

- Scope: Lint compliance refactor -- file renames to kebab-case, import path updates, ESLint config tightening, trivial null->undefined fix
- Additional risks from uncommitted changes: Import path breakage (mitigated by tests and lint), ESLint CI failures (self-correcting)
- **Cumulative residual risk: 4/25 (Low)**

### Pipeline Summary

| Layer         | Scope                                                                                      | Residual Risk   |
| ------------- | ------------------------------------------------------------------------------------------ | --------------- |
| Unreleased    | Docs updates, changeset metadata                                                           | 1/25 (Very Low) |
| + Unpushed    | No additional changes                                                                      | 1/25 (Very Low) |
| + Uncommitted | File renames to kebab-case, import path updates, ESLint config tightening, null->undefined | 4/25 (Low)      |

All scores are below the risk appetite threshold of 5. This is a risk-neutral refactoring action -- it enforces stricter lint rules and aligns filenames with the kebab-case convention without changing any runtime behaviour.

RISK_SCORES: commit=4 push=1 release=1
RISK_BYPASS: reducing
