# Pipeline Risk Report

**Date:** 2026-03-31T05:25:17Z
**Policy last reviewed:** 2026-03-30 (current)

## Layer 1: Unreleased Changes (release risk)

**Scope:** v2.0.0 major release -- Node.js >=22, opensearch v3, waycharter v2, got-to-fetch migration, 20+ dependency updates. Changeset already exists (`.changeset/update-dependencies.md`).

#### Risk 1: Runtime regression from major dependency upgrades
- Inherent impact: 4/5 (Significant) -- opensearch v3 and got-to-fetch touch the address search and API response paths; incorrect results or failures would affect RapidAPI consumers
- Inherent likelihood: 4/5 (Likely) -- 20+ dependency upgrades spanning the search engine client, HTTP library, and Node.js runtime version represent high complexity with wide-reaching behaviour changes
- Inherent risk: 16/25 (High)
- Controls:
  - CI test suite (719 tests including cucumber BDD scenarios in `test/js/steps.js`) -- will-execute on push -- these exercise the opensearch query API and address search paths. Reduces likelihood from 4 to 2 because the BDD scenarios cover address search, autocomplete, and API response shapes against a real opensearch instance in CI.
  - Release preview deploy -- will-execute before merge -- reduces likelihood further by validating the built artefact in a preview environment. However, preview environment may differ from production (cold cache, different data volume). Provides partial validation only.
- Residual impact: 4/5 (Significant)
- Residual likelihood: 2/5 (Unlikely)
- **Residual risk: 8/25 (Medium)**

**Layer 1 residual risk: 8/25 (Medium)**

## Layer 2: Unreleased + Unpushed (push risk)

**Scope:** Layer 1 scope plus 5 unpushed commits: governance framework (agents, hooks, skills, ADRs, architecture docs, C4 model, risk policy, briefing system, retrospective skill). Also adds `bats` as a devDependency, `push:watch`/`release:watch`/`test:hooks` npm scripts, and CI governance shell scripts.

**Additional risks from unpushed changes:**

#### Risk 2: Dev dependency or script change affects published package
- Inherent impact: 2/5 (Minor) -- devDependencies and npm scripts do not enter the published package or Docker image runtime; worst case is a broken developer workflow
- Inherent likelihood: 1/5 (Rare) -- `bats` is a well-known test runner added as devDependency only; scripts are bash wrappers for existing git/gh commands
- Inherent risk: 2/25 (Very Low)
- Controls: none needed at this risk level
- Residual impact: 2/5 (Minor)
- Residual likelihood: 1/5 (Rare)
- **Residual risk: 2/25 (Very Low)**

The unpushed governance files (.claude/, docs/, AGENTS.md, etc.) are risk-neutral -- they do not affect the npm package, Docker image, or AWS deployment. They cannot cause runtime regression.

**Layer 2 cumulative residual risk: 8/25 (Medium)** (driven entirely by Layer 1)

## Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

**Scope:** Layer 2 scope plus 4 staged files, all in `.claude/hooks/`:
- `briefing-inject.sh` -- session-marker optimisation (inject briefing only on first prompt)
- `risk-score-commit-gate.sh` -- added RISK-POLICY.md staleness check (pure bash, no LLM call)
- `risk-score-reset.sh` -- added briefing marker cleanup
- `risk-score.sh` -- gutted to near no-op (WIP marker + report rotation only)

**Additional risks from uncommitted changes:**

#### Risk 3: Hook script defect blocks developer workflow
- Inherent impact: 1/5 (Negligible) -- `.claude/hooks/` scripts are developer tooling configuration; a defect would only affect the Claude Code session, not the build, publish, or runtime paths
- Inherent likelihood: 2/5 (Unlikely) -- the changes simplify rather than add complexity (risk-score.sh reduced from 247 to 24 lines); session marker pattern is straightforward
- Inherent risk: 2/25 (Very Low)
- Controls:
  - `bats` tests in `.claude/hooks/test/risk-gate.bats` -- will-execute via `npm run test:hooks` -- exercises the gate helper functions. Reduces likelihood from 2 to 1.
- Residual impact: 1/5 (Negligible)
- Residual likelihood: 1/5 (Rare)
- **Residual risk: 1/25 (Very Low)**

**Layer 3 cumulative residual risk: 8/25 (Medium)** (driven entirely by Layer 1)

## Pipeline Summary

| Layer | Scope | Residual Risk |
|-------|-------|--------------|
| Unreleased | v2.0.0 major: opensearch v3, got-to-fetch, Node >=22, 20+ deps | 8/25 (Medium) |
| + Unpushed | Governance framework, agents, hooks, ADRs, docs, bats devDep | 8/25 (Medium) |
| + Uncommitted | 4 .claude/hooks/ scripts (simplification + staleness check) | 8/25 (Medium) |

## Risk-Neutral Bypass

The uncommitted changes (Layer 3 delta) are **risk-neutral**: all 4 files are `.claude/hooks/` shell scripts that do not affect the npm package, Docker image, CI pipeline, or runtime path. They cannot change cumulative release risk.

Bypass markers written:
- `/tmp/risk-reducing-commit-22c156c0-b50d-4187-a362-9b6ef431850c` -- commit bypass (risk-neutral)
- `/tmp/risk-reducing-push-22c156c0-b50d-4187-a362-9b6ef431850c` -- push bypass (risk-neutral, all unpushed changes are governance/docs/devDeps)

## Suggested Actions

- The release queue (Layer 1) drives all cumulative risk at 8/25 (Medium). The uncommitted and unpushed layers add no additional release risk.
- Before releasing v2.0.0, ensure CI BDD tests pass against opensearch v3 and verify the release preview deploy is healthy.
- Consider running smoke tests against the preview environment specifically for address search and autocomplete endpoints.

## Pipeline Flow

- Commit and push are clear -- the staged changes are risk-neutral governance files and bypass markers are in place.
- The release queue remains at Medium risk from the v2.0.0 major upgrade. Address this through CI validation and preview verification before merging the release PR.
