Now I have a clear picture. Let me produce the risk report.

## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

**Scope:** Recent commits on master (5dff8bc through earlier commits) include infrastructure and tooling changes: deterministic score writing, session-scoped tmp dirs, risk-scorer refactoring, gate file path redaction, ERR traps, shared JSON helpers, lifecycle tests, and the BRIEFING.md update. These are all developer tooling and governance infrastructure -- none affect the npm package, Docker image, or live API.

- Scope: Developer tooling, CI hooks, and governance docs
- Risks: None material -- changes do not touch runtime code, deployment config, or published artifacts
- **Residual risk: 1/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

**Scope:** Same as Layer 1. No additional unpushed commits beyond what is on master. The git status shows no unpushed commits (HEAD is at 5dff8bc on master).

- Additional risks from unpushed changes: None
- **Cumulative residual risk: 1/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

**Scope:** 5 staged files adding safety controls: health endpoint, v2 API test in CI pipeline, rolling deployment with rollback, post-deploy smoke tests, and BRIEFING.md update.

#### Risk 1: Health endpoint in production code (waycharterServer.js)

New `/health` resource registered via WayCharter, returning `{status, version, timestamp}` with `no-cache` header. Root `/` now includes a health link.

- Inherent impact: 4/5 (Significant) - Changes to the production API server code could break existing address search/autocomplete
- Inherent likelihood: 1/5 (Rare) - The health endpoint is additive only. It registers a new path `/health` and adds one link to the root resource. No existing routes or loaders are modified. WayCharter's `registerResourceType` is the same pattern used for the existing index route.
- Inherent risk: 4/25 (Low)
- Controls:
  - `test/resources/features/addressv2.feature` "Health Check" scenario - exercises the health endpoint via HATEOAS link traversal, verifying it returns with `cache-control: no-cache`. Catches registration failures.
  - `test/resources/features/addressv2.feature` "Root API" scenario - updated to verify the health link appears in root response alongside existing links. Catches root resource regression.
  - `test:rest2:nogeo` script now in `test:nogeo` pipeline - these scenarios will actually run in CI, reducing likelihood from 1 to 1 (already rare, confirmed by tests)
- **Residual risk: 2/25 (Very Low)**

#### Risk 2: Deployment policy change (main.tf)

Changed from AllAtOnce to Rolling deployment, enabled RollbackLaunchOnFailure, changed health check target from `HTTP:80/` to `HTTP:80/health`.

- Inherent impact: 5/5 (Severe) - Misconfigured deployment could cause production outage
- Inherent likelihood: 2/5 (Unlikely) - Rolling deployment is a safer strategy than AllAtOnce. RollbackLaunchOnFailure=true adds automatic rollback. However, the health check now targets `/health` which must exist in both the current deployed version AND the new version during rolling updates.
- Inherent risk: 10/25 (High)
- Controls:
  - Rolling deployment with MinInstancesInService=2 means old instances stay up while new ones are validated
  - RollbackLaunchOnFailure=true automatically reverts if new instances fail health checks
  - The health endpoint is being added in the same commit, so the next deploy will have it
  - **Critical consideration**: The CURRENT production version does NOT have `/health`. During the first rolling deploy after this change, new instances will have `/health` but old instances won't. However, the ELB health check targets individual instances, and old instances responding on `/` will start failing the new health check at `HTTP:80/health`. This could cause the ELB to mark healthy old instances as unhealthy during the transition.
  - Wait: Actually, the health check change and the rolling deploy change go out together in the Terraform apply. During a rolling update, old instances serve on `/` and new instances serve on `/health`. The ELB health check `HTTP:80/health` would fail against old instances. BUT: the EB health check for rolling deployment validation checks the NEW instances, not old ones. The ELB health check change affects ALL instances. This is a real risk.
  - Mitigation: MinInstancesInService=2 and RollbackLaunchOnFailure=true provide automated recovery. If old instances are marked unhealthy by the ELB, EB's rolling update won't terminate them until new ones pass. The 120s stabilization wait + smoke test in CI adds detection.
  - Post-deploy smoke test (`release.yml`) - `curl -sf --retry 3 --retry-delay 10 https://backend.addressr.io/health` catches complete deployment failure
- Residual impact: 4/5, Residual likelihood: 1/5 (the rolling deploy + rollback makes complete outage rare; partial disruption during transition is possible but brief)
- **Residual risk: 4/25 (Low)**

#### Risk 3: CI workflow change (release.yml)

Added post-deploy smoke test steps: 120s wait, then curl health + root endpoints with retries.

- Inherent impact: 2/5 (Minor) - Only affects CI/CD pipeline, not runtime
- Inherent likelihood: 1/5 (Rare) - Simple curl commands with retries, well-understood pattern
- Inherent risk: 2/25 (Very Low)
- Controls: The smoke test is additive. If it fails, the CI job fails visibly but the deployment has already happened. It provides detection, not prevention.
- **Residual risk: 1/25 (Very Low)**

#### Risk 4: BRIEFING.md update

- Inherent impact: 1/5 (Negligible) - Documentation only
- Inherent likelihood: 1/5 (Rare)
- **Residual risk: 1/25 (Very Low)**

**Combined Layer 3 assessment:** The dominant risk is the deployment configuration change (Risk 2 at 4/25). All other changes are Very Low. The overall posture is risk-REDUCING: this commit closes the #1 risk gap (v2 API untested in CI), adds a proper health check endpoint, enables rolling deployment with automatic rollback, and adds post-deploy smoke tests.

- **Cumulative residual risk: 4/25 (Low)**

### Pipeline Summary

| Layer | Scope | Residual Risk |
|-------|-------|--------------|
| Unreleased | Developer tooling, governance docs | 1/25 (Very Low) |
| + Unpushed | No additional changes | 1/25 (Very Low) |
| + Uncommitted | Health endpoint, v2 CI tests, rolling deploy, smoke tests, BRIEFING.md | 4/25 (Low) |

### Risk-Reducing Assessment

This commit is **risk-reducing**. It directly addresses the #1 documented risk gap (v2 API untested in CI) and replaces the dangerous AllAtOnce deployment with rolling deployment + automatic rollback + health checks + post-deploy smoke tests. Every change adds safety controls that did not previously exist.

### Deployment Transition Note

The ELB health check changing from `HTTP:80/` to `HTTP:80/health` while old instances lack `/health` is a transitional concern. During the first rolling deploy, old instances may briefly fail the new health check. The rolling update logic, MinInstancesInService=2, and RollbackLaunchOnFailure=true mitigate this. Monitor the first deployment after this change closely.

RISK_SCORES: commit=4 push=1 release=1
RISK_BYPASS: reducing
