## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

Looking at recent commits, the last release was `b08ad26 chore: release`. Since then, there are unreleased commits including documentation changes (ADR 021, README restructure) and a changeset for a patch release.

- Scope: Documentation updates, changeset for patch release (dep upgrades + waychaser fix)
- Risks: Minimal — docs and changeset metadata only
- **Residual risk: 1/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

The git status shows many deleted files under `.claude/agents/` (shown as ` D` — deleted in working tree but not staged). No unpushed commits are indicated beyond what is already on master.

- Scope: Same as Layer 1 (no additional unpushed commits)
- Additional risks from unpushed changes: None
- **Cumulative residual risk: 1/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

- Scope: Staged change to `eslint.config.js` adding three warn-level lint rules (max-lines-per-function, max-depth, max-params)

#### Risk 1: Unintended CI breakage from new lint rules

- Inherent impact: 1/5 (Negligible) — warn-level rules produce warnings only, no exit code change
- Inherent likelihood: 1/5 (Rare) — ESLint warn severity does not fail builds by definition
- Inherent risk: 1/25 (Very Low)
- Controls:
  - Rules are set to `warn` not `error` — reduces likelihood to 1 because warnings cannot cause non-zero exit
- **Residual risk: 1/25 (Very Low)**

#### Risk 2: Config syntax error in eslint.config.js

- Inherent impact: 2/5 (Minor) — broken ESLint config would fail lint step in CI
- Inherent likelihood: 1/5 (Rare) — adding standard rule entries is straightforward
- Inherent risk: 2/25 (Very Low)
- Controls:
  - CI lint step would catch a malformed config before merge
- **Residual risk: 1/25 (Very Low)**

This is a **risk-neutral** change: it is developer tooling configuration with no production code, no UI, no data, and no dependency changes.

### Pipeline Summary

| Layer         | Scope                                 | Residual Risk   |
| ------------- | ------------------------------------- | --------------- |
| Unreleased    | Docs, changeset metadata              | 1/25 (Very Low) |
| + Unpushed    | No additional changes                 | 1/25 (Very Low) |
| + Uncommitted | eslint.config.js — 3 warn-level rules | 1/25 (Very Low) |

No back-pressure warnings needed. No confidential information detected. No suggested actions required.

RISK_SCORES: commit=1 push=1 release=1
RISK_BYPASS: reducing
