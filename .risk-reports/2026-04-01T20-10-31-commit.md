I have enough context. The staged changes are:

1. `release.yml` — `continue-on-error: true` on check-deps job (advisory per ADR 015)
2. `push-and-watch.sh` — checks release/build job conclusions instead of `--exit-status` on `gh run watch`
3. `release-watch.sh` — same pattern for release job

The unstaged change to `docs/BRIEFING.md` is separate (already committed in the most recent commit per git log).

## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

Recent commits include infrastructure/tooling changes (deterministic score writing, risk-scorer split, hook improvements, retrospective). No application code changes. No npm package or Docker image changes.

- Scope: CI/CD workflow and script improvements, developer tooling only
- Risks: None material. These are developer infrastructure changes that do not affect the npm package, Docker image, or live AWS service.
- **Residual risk: 1/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

No unpushed commits beyond what is already on remote (the staged changes are uncommitted, not unpushed).

- Scope: Same as Layer 1
- Additional risks from unpushed changes: None
- **Cumulative residual risk: 1/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

Three staged files plus one modified unstaged file.

#### Risk 1: Advisory check-deps failure could mask a real issue
- Inherent impact: 2/5 (Minor) — check-deps is informational; it flags stale dependencies but does not gate releases. Even if completely ignored, existing service continues operating.
- Inherent likelihood: 1/5 (Rare) — `continue-on-error: true` is the standard GitHub Actions pattern for advisory jobs. The scripts already comment "check-deps is advisory per ADR 015."
- Inherent risk: 2/25 (Very Low)
- Controls:
  - ADR 015 explicitly defines check-deps as advisory, not a gate
  - The scripts (`push-and-watch.sh` lines 67-74, `release-watch.sh` lines 128-135) specifically check `release` and `build` job conclusions for failure, not overall run status — this ensures real failures are still caught
- **Residual risk: 1/25 (Very Low)**

#### Risk 2: Script logic change could miss a genuine build/release failure
- Inherent impact: 3/5 (Moderate) — if the scripts incorrectly report success on a failed build/release, a broken push could go unnoticed by the developer
- Inherent likelihood: 1/5 (Rare) — the new logic explicitly checks `release` and `build` job conclusions for `"failure"` string match, which is the correct GitHub API value. The `|| true` on `gh run watch` is correct because the exit status reflects advisory job failures.
- Inherent risk: 3/25 (Low)
- Controls:
  - `push-and-watch.sh` line 69: explicit check `if [ "$RELEASE_CONCLUSION" = "failure" ] || [ "$BUILD_CONCLUSION" = "failure" ]` — catches both critical job failures
  - `release-watch.sh` line 130: explicit check `if [ "$RELEASE_CONCLUSION" = "failure" ]` — catches release job failure
  - Both scripts call `show_failure_guidance` and `exit 1` on failure, preserving the error path
- **Residual risk: 2/25 (Very Low)**

#### Risk 3: BRIEFING.md modification (unstaged)
- Inherent impact: 1/5 (Negligible) — documentation only
- Inherent likelihood: 1/5 (Rare) — no runtime effect
- Inherent risk: 1/25 (Very Low)
- **Residual risk: 1/25 (Very Low)**

#### Confidential Information Disclosure
No business metrics, revenue figures, user counts, or pricing data present in any of the changes.

### Pipeline Summary

| Layer | Scope | Residual Risk |
|-------|-------|--------------|
| Unreleased | CI/CD tooling commits | 1/25 (Very Low) |
| + Unpushed | No additional | 1/25 (Very Low) |
| + Uncommitted | Advisory check-deps handling in 3 files | 2/25 (Very Low) |

### Assessment

This change is **risk-reducing**. It fixes false failures where the advisory check-deps job was causing pipeline scripts to report failure even though the actual build and release jobs succeeded. The change aligns implementation with the intent documented in ADR 015.

RISK_SCORES: commit=2 push=1 release=1
RISK_BYPASS: reducing
