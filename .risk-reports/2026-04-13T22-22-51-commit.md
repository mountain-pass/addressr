Good. The `engines` field confirms `node >= 22`, and the eslint config no longer has the `unicorn/prefer-global-this: 'off'` override (meaning the rule is now active via the recommended config, enforcing `globalThis`).

## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

- Scope: Recent commits include changeset for patch release (dep upgrades + waychaser fix), ADR 021, README restructuring, plus this lint refactor if committed and pushed.
- Risks: The unreleased queue contains documentation and dependency changes already released as v2.0.3 per the merge of changeset-release. No additional functional risk from prior unreleased items.
- **Residual risk: 1/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

- Scope: No unpushed commits beyond what is already on remote (per recent commits shown).
- Additional risks from unpushed changes: None.
- **Cumulative residual risk: 1/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

- Scope: 9 files — lint config change removing one rule override, plus 8 source/test files with mechanical `global.X` to `globalThis.X` replacements.

#### Risk 1: globalThis substitution causes runtime regression

- Inherent impact: 3/5 (Moderate) — affects server startup and test execution across multiple modules
- Inherent likelihood: 1/5 (Rare) — `global` and `globalThis` are the same object reference in Node.js >= 12; project requires Node.js >= 22
- Inherent risk: 3/25 (Low)
- Controls:
  - Node.js specification guarantees `global === globalThis` — reduces likelihood to effectively 0; this is a language-level invariant, not a code-level control
  - `/Users/tomhoward/Projects/addressr/test/js/steps.js` and `/Users/tomhoward/Projects/addressr/test/js/world.js` — the test files themselves are updated, and the existing test suite exercises the affected code paths (server startup, address service, elasticsearch client). If any substitution broke behavior, tests would fail.
  - ESLint rule `unicorn/prefer-global-this` now enforced — prevents future regression to `global` usage
- **Residual risk: 1/25 (Very Low)**

#### Risk 2: ESLint config change introduces unexpected lint failures

- Inherent impact: 1/5 (Negligible) — lint failures block CI but do not affect runtime
- Inherent likelihood: 1/5 (Rare) — the auto-fix was applied to all files; removing the override simply enforces what was already fixed
- Inherent risk: 1/25 (Very Low)
- Controls:
  - All source files already converted to `globalThis` — no remaining violations expected
- **Residual risk: 1/25 (Very Low)**

- **Cumulative residual risk: 1/25 (Very Low)**

### Pipeline Summary

| Layer         | Scope                                              | Residual Risk   |
| ------------- | -------------------------------------------------- | --------------- |
| Unreleased    | docs + dep patch release                           | 1/25 (Very Low) |
| + Unpushed    | no additional                                      | 1/25 (Very Low) |
| + Uncommitted | lint refactor: global -> globalThis across 9 files | 1/25 (Very Low) |

### Assessment

This is a **risk-neutral** change. It is a mechanical, semantically identical substitution enforced by a lint rule, applied to a project that requires Node.js >= 22 where `global === globalThis` is guaranteed. No business logic, API surface, or runtime behavior is altered.

RISK_SCORES: commit=1 push=1 release=1
RISK_BYPASS: reducing
