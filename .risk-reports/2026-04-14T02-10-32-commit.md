## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

This is a patch release consisting entirely of lint compliance refactoring: ESLint rule enablement, auto-fixes (prettier, globalThis, variable renames), file renames to kebab-case, async/await refactoring of promise chains, and replacement of deprecated `url.parse()` with `new URL()`.

#### Risk 1: Behavioural regression from code refactoring

- Inherent impact: 4/5 (Significant) - If async/await refactoring or `url.parse` to `new URL()` changes break error handling or URL parsing, API consumers get incorrect results or errors
- Inherent likelihood: 2/5 (Unlikely) - These are well-understood mechanical transformations, but async/await refactoring and URL constructor differences (e.g. relative URLs, error throwing) have known edge cases
- Inherent risk: 8/25 (Medium)
- Controls:
  - CI test suite (integration and unit tests covering address search/autocomplete API endpoints) - reduces likelihood from 2 to 1 because test failures would catch broken URL parsing or broken promise chains before merge
- **Residual risk: 4/25 (Low)**

#### Risk 2: File rename breaking imports

- Inherent impact: 4/5 (Significant) - Broken imports would prevent the service from starting
- Inherent likelihood: 1/5 (Rare) - File renames are 3 files; any broken import would fail immediately at startup and in CI
- Inherent risk: 4/25 (Low)
- Controls:
  - CI test suite (server startup and integration tests) - reduces likelihood from 1 to 1 (already rare); would catch immediately
- **Residual risk: 4/25 (Low)**

#### Risk 3: Variable rename introducing subtle bugs

- Inherent impact: 4/5 (Significant) - `swaggerDoc` to `swaggerDocument` across 3 files could miss a reference
- Inherent likelihood: 1/5 (Rare) - ESLint auto-fix handles scope-aware renaming; any missed reference would cause a ReferenceError caught by tests
- Inherent risk: 4/25 (Low)
- Controls:
  - CI test suite - catches ReferenceError at runtime
- **Residual risk: 4/25 (Low)**

**Layer 1 residual risk: 4/25 (Low)** - Dominated by the async/await and URL constructor refactoring risk, mitigated by CI tests.

### Layer 2: Unreleased + Unpushed (push risk)

The unpushed changes ARE the unreleased changes (11 commits + changeset). No additional scope beyond Layer 1.

- Additional risks from unpushed changes: None beyond Layer 1
- **Cumulative residual risk: 4/25 (Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

From the git status, the uncommitted changes are deletions of `.claude/agents/*.md` files. These are developer tooling configuration files (agent definitions) and do not affect build, publish, or runtime paths.

- Additional risks from uncommitted changes: None. Agent definition files are developer tooling with negligible impact.
- **Cumulative residual risk: 4/25 (Low)**

### Pipeline Summary

| Layer         | Scope                                                                                                   | Residual Risk |
| ------------- | ------------------------------------------------------------------------------------------------------- | ------------- |
| Unreleased    | Lint cleanup patch: ESLint rules, async/await refactor, URL constructor, file renames, variable renames | 4/25 (Low)    |
| + Unpushed    | Same as unreleased (11 commits + changeset)                                                             | 4/25 (Low)    |
| + Uncommitted | Deletion of .claude/agents/\*.md files (dev tooling)                                                    | 4/25 (Low)    |

This is a risk-reducing action overall: it eliminates deprecated API usage (`url.parse`), adds lint guardrails (max-lines, max-depth, max-params), and resolves all ESLint warnings, improving code quality and maintainability.

RISK_SCORES: commit=4 push=4 release=4
RISK_BYPASS: reducing
