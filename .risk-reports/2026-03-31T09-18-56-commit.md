## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

- Scope: 9 unpushed commits adding the full governance framework (hooks, agents, skills, problem tickets, architecture docs, scripts). 187 files, 30,622 insertions. No source code, CI, Docker, or production-affecting files changed. Only `.claude/`, `docs/`, and `scripts/` directories.
- Risks:

#### Risk 1: Governance hooks interfere with development workflow
- Inherent impact: 2/5 (Minor) - hooks only affect developer experience, not the npm package, Docker image, or live API
- Inherent likelihood: 2/5 (Unlikely) - hooks are fail-open on error (set -e with exit 0 fallbacks), tested with 41 bats tests
- Inherent risk: 4/25 (Low)
- Controls:
  - `.claude/hooks/test/lifecycle.bats` (31 tests) and `.claude/hooks/test/risk-gate.bats` (10 tests) - exercises score writing, bypass markers, plan verdicts, cleanup, gate thresholds, TTL, drift detection. Catches regressions in gate logic before they block workflows.
- **Residual risk: 2/25 (Very Low)**

- **Residual risk: 2/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

- Scope: Same as Layer 1 -- all unpushed commits are also unreleased. No additional unreleased-but-pushed changes exist.
- Additional risks from unpushed changes: None beyond Layer 1.
- **Cumulative residual risk: 2/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

- Scope: Refactoring 35 files in the governance hook system. Moves score file writing from non-deterministic agent Bash calls to a single deterministic PostToolUse hook (`risk-score-mark.sh`). Removes `risk-policy-mark-reviewed.sh`. Consolidates flat `/tmp/` files into session-scoped `$TMPDIR/claude-risk-$SESSION_ID/` directories. Removes 11 obsolete problem tickets. Updates all hooks to use shared `_parse_input` / `_risk_dir` helpers. 378 insertions, 993 deletions (net -615 lines).
- Additional risks from uncommitted changes:

#### Risk 2: New PostToolUse hook fails to parse agent output correctly
- Inherent impact: 2/5 (Minor) - if parsing fails, gates block (fail-closed) and the developer must re-run the scorer. No production impact.
- Inherent likelihood: 2/5 (Unlikely) - parsing uses simple grep/cut on well-defined structured markers (`RISK_SCORES: commit=N push=N release=N`). 41 tests pass including specific parsing tests in lifecycle.bats.
- Inherent risk: 4/25 (Low)
- Controls:
  - `lifecycle.bats::pipeline: RISK_SCORES line writes all three score files` - verifies exact parsing of the RISK_SCORES line format
  - `lifecycle.bats::pipeline: RISK_BYPASS reducing creates all bypass markers` - verifies bypass parsing
  - `lifecycle.bats::pipeline: RISK_BYPASS incident creates incident marker` - verifies incident bypass
  - `lifecycle.bats::pipeline: missing RISK_SCORES line writes no files` - verifies fail-closed on missing output
  - `lifecycle.bats::pipeline: scores written to session-scoped dir are found by gate` - end-to-end integration of new paths with existing gate logic
- **Residual risk: 2/25 (Very Low)**

#### Risk 3: Session-scoped directory migration breaks existing hooks
- Inherent impact: 2/5 (Minor) - would cause hooks to look in wrong paths, blocking workflow until fixed. No production impact.
- Inherent likelihood: 1/5 (Rare) - all hooks consistently use `_risk_dir()` helper, single source of truth for path construction. Tests validate the new paths work with gates.
- Inherent risk: 2/25 (Very Low)
- Controls:
  - `lifecycle.bats::helper: _risk_dir creates directory` - verifies directory creation
  - `lifecycle.bats::cleanup: rm -rf session dir removes all files` - verifies cleanup works
  - `risk-gate.bats` (10 tests) - all gate tests use session-scoped dirs and pass
- **Residual risk: 1/25 (Very Low)**

- **Cumulative residual risk: 2/25 (Very Low)**

### Risk-Reducing Assessment

This change is **risk-reducing**. It directly addresses two documented problems:
- P001 (risk-scorer not writing gate scores) - eliminated by moving score writing out of agent control entirely
- P002 (risk gate score bypass via direct file write) - mitigated by removing Bash tool from scorer agents so they cannot write files; only the PostToolUse hook writes scores based on structured output markers

Net effect: -615 lines, removal of fragile Python backup parsing, consolidation of 15+ flat `/tmp/` files into a single session-scoped directory with simplified cleanup.

### Confidential Information Disclosure

No business metrics, revenue figures, user counts, pricing details, or traffic volumes found in the diff.

### Downstream Back-Pressure

- Adding this commit to the push queue: push cumulative risk = 2/25 (Very Low). No warning needed.
- Push queue into release queue: release cumulative risk = 2/25 (Very Low). No warning needed.

### Pipeline Summary

| Layer | Scope | Residual Risk |
|-------|-------|--------------|
| Unreleased | Governance framework (hooks, agents, docs) | 2/25 (Very Low) |
| + Unpushed | Same (no additional) | 2/25 (Very Low) |
| + Uncommitted | Hook refactor: deterministic score writing, session-scoped dirs | 2/25 (Very Low) |

RISK_SCORES: commit=2 push=2 release=2
RISK_BYPASS: reducing
