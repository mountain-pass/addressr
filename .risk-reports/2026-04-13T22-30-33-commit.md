## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

The unreleased queue contains the most recent release (v2.0.0 per recent commits showing `chore: release`). No additional unreleased changes beyond what is currently being staged.

- Scope: No unreleased changes beyond the current commit being assessed.
- **Residual risk: 0/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

- Scope: No unpushed commits beyond the current staging.
- **Cumulative residual risk: 0/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

- Scope: 10 files changed — ESLint rule promotion (`unicorn/prevent-abbreviations` warn to error), mechanical variable renames across 8 source/test/CI files, and 1 new lint validation test.

#### Risk 1: Cross-file rename of `swaggerDocument` via globalThis

- Inherent impact: 4/5 (Significant) — If any reference to `globalThis.swaggerDocument` is missed, the API root and address endpoints would fail at runtime, breaking RapidAPI consumers.
- Inherent likelihood: 2/5 (Unlikely) — The rename spans only 3 files (swagger.js, DefaultService.js, AddressrEmbeddedDriver.js) and is a straightforward find-replace. The globalThis property is set in swagger.js and consumed in DefaultService.js; both are updated consistently.
- Inherent risk: 8/25 (Medium)
- Controls:
  - `test/lint/abbreviations.test.js` — New test runs ESLint with `--format json` and asserts zero `unicorn/prevent-abbreviations` violations. This would catch any remaining `swaggerDoc` abbreviation. Reduces likelihood from 2 to 1.
  - `test/js/drivers/AddressrEmbeddedDriver.js` — The embedded driver test imports `swaggerDocument` directly from swagger.js and exercises API root/address endpoints, so a broken rename would fail the existing integration test suite. Reduces likelihood from 2 to 1.
  - ESLint rule at `error` level — Any leftover `res`, `dir`, `def` abbreviation would now fail the lint step in CI. Reduces likelihood from 2 to 1.
- **Residual risk: 4/25 (Low)**

#### Risk 2: Express callback parameter renames (`res` to `response`/`response_`)

- Inherent impact: 4/5 (Significant) — If a rename is incomplete within a function scope, the Express error handler or controller would throw a ReferenceError at runtime.
- Inherent likelihood: 1/5 (Rare) — These are function-scoped renames (single function bodies in controllers/Default.js, swagger.js, utils/stream-down.js). The ESLint error rule would flag any remaining `res` usage.
- Inherent risk: 4/25 (Low)
- Controls:
  - ESLint `unicorn/prevent-abbreviations` at error level with `res: { response: true }` — catches any leftover `res` references.
  - Existing integration/API tests exercise the Express routes.
- **Residual risk: 1/25 (Very Low)**

#### Risk 3: Local variable renames in address-service.js and CI pipeline

- Inherent impact: 3/5 (Moderate) — address-service.js is core business logic (G-NAF loading, directory resolution). A broken rename could prevent address data loading.
- Inherent likelihood: 1/5 (Rare) — These are all function-scoped local variable renames (`def`→`defaults`, `currentDir`→`currentDirectory`, etc.). ESLint would catch inconsistencies.
- Inherent risk: 3/25 (Low)
- Controls:
  - ESLint `unicorn/prevent-abbreviations` at error with `dir: { directory: true }` — catches any leftover abbreviations.
  - Existing test suite exercises address loading paths.
- **Residual risk: 1/25 (Very Low)**

**Combined Layer 3 cumulative residual risk: 4/25 (Low)**

All changes are mechanical variable renames enforcing a new lint rule. No behavioral changes. The new test validates the lint rule holds. This is a code quality improvement — risk-reducing.

### Pipeline Summary

| Layer         | Scope                                                    | Residual Risk   |
| ------------- | -------------------------------------------------------- | --------------- |
| Unreleased    | None pending                                             | 0/25 (Very Low) |
| + Unpushed    | None pending                                             | 0/25 (Very Low) |
| + Uncommitted | ESLint rule promotion + variable renames across 10 files | 4/25 (Low)      |

No confidential information detected in diffs. All scores below the risk appetite threshold of 5.

RISK_SCORES: commit=4 push=0 release=0
RISK_BYPASS: reducing
