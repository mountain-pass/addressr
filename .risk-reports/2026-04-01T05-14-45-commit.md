I have enough context now. Let me assess the full pipeline state.

## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

**Scope:** v1.1.4 patch release. Changeset `add-risk-controls.md` covers: `/health` HATEOAS endpoint, `test:rest2:nogeo` in CI, rolling deployment with rollback, post-deploy smoke tests, ngrok removal, bats addition. The Terraform config (`deploy/main.tf`) already shows `DeploymentPolicy=Rolling`, `MinInstancesInService=2`, `RollbackLaunchOnFailure=true`, `RollingUpdateEnabled=true`. The release workflow (`release.yml`) includes a 120s stabilization wait and curl-based smoke tests against `backend.addressr.io/health` and `/`.

#### Risk 1: Deployment causes transient outage during rolling update
- Inherent impact: 4/5 (Significant) - RapidAPI consumers affected if deploy fails
- Inherent likelihood: 2/5 (Unlikely) - This is a controls-only release, no application logic changes
- Inherent risk: 8/25 (Medium)
- Controls:
  - `deploy/main.tf` rolling deploy with `MinInstancesInService=2` - reduces impact from 4 to 3 because at least 2 instances remain serving during deploy
  - `deploy/main.tf` `RollbackLaunchOnFailure=true` - reduces likelihood from 2 to 1 because failed launches auto-rollback
  - `.github/workflows/release.yml` lines 76-89 smoke tests - reduces likelihood from 2 to 1 because post-deploy verification catches broken deployments
  - ELB health check at `HTTP:80/health` with 10s interval - reduces impact from 4 to 3 because unhealthy instances removed from rotation
- **Residual risk: 3/25 (Low)**

#### Risk 2: npm publish of v1.1.4 breaks self-hosted installations
- Inherent impact: 4/5 (Significant) - npm consumers cannot install or run addressr
- Inherent likelihood: 1/5 (Rare) - No application logic changes; only deployment config, health endpoint, and test additions
- Inherent risk: 4/25 (Low)
- Controls:
  - CI runs `test:nogeo` and now `test:rest2:nogeo` - validates both v1 and v2 API paths before publish
  - Changeset is a patch (no semver-breaking changes)
- **Residual risk: 2/25 (Very Low)**

#### Risk 3: /health endpoint reveals sensitive information
- Inherent impact: 3/5 (Moderate) - Could expose version or internal state
- Inherent likelihood: 1/5 (Rare) - Standard health endpoint returning status + version; no credentials or business metrics
- Inherent risk: 3/25 (Low)
- Controls:
  - Health endpoint is HATEOAS-linked from root (standard pattern)
  - Smoke test validates it returns expected response
- **Residual risk: 1/25 (Very Low)**

**Layer 1 aggregate residual risk: 3/25 (Low)**

The release is fundamentally risk-reducing: it adds rolling deploy, rollback, v2 test coverage in CI, and smoke tests -- all controls that did not exist in v1.1.3.

### Layer 2: Unreleased + Unpushed (push risk)

**Scope:** Recent commits on master (5dff8bc retrospective/BRIEFING.md update, 6ae9084 deterministic score writing, c466b98 lifecycle tests, 9b38035 risk-scorer split, 7b82814 gate file path redaction). These are all developer tooling and governance changes. No application code or deployment config in unpushed commits.

- Additional risks from unpushed changes: None material. All changes are to hook scripts, risk-scorer agents, and documentation -- Impact 1 (Negligible) for all.
- **Cumulative residual risk: 3/25 (Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

**Scope:** Uncommitted changes are:
- `docs/BRIEFING.md` (modified) - documentation only
- `.risk-reports/` (untracked) - risk report artifacts, not deployed

- Additional risks from uncommitted changes: Zero. Documentation and local risk reports have no build, publish, or runtime impact.
- **Cumulative residual risk: 3/25 (Low)**

### Confidential Information Disclosure Check

No business metrics (revenue, user counts, pricing, traffic volumes) found in any layer of changes.

### Pipeline Summary

| Layer | Scope | Residual Risk |
|-------|-------|--------------|
| Unreleased | v1.1.4: health endpoint, v2 CI tests, rolling deploy, smoke tests, ngrok removal | 3/25 (Low) |
| + Unpushed | Tooling: risk-scorer split, deterministic scoring, lifecycle tests, retrospective | 3/25 (Low) |
| + Uncommitted | Docs: BRIEFING.md update, risk-reports dir | 3/25 (Low) |

### Risk-Reducing Assessment

This release is **risk-reducing**. It adds controls that did not exist in v1.1.3:
1. Rolling deployment replaces AllAtOnce (BRIEFING.md previously noted "No rolling deploy, no automated rollback")
2. Automatic rollback on failure
3. v2 API tests in CI (previously flagged as "#1 risk gap" in BRIEFING.md)
4. Post-deploy smoke tests (new)
5. Removal of broken ngrok dependency

All of these reduce the residual risk profile of the production system going forward.

### Downstream Back-Pressure

- Push would not push release queue risk above 5 (currently 3). No warning needed.
- Release at 3 is within appetite (threshold: 5). No remediation required.

RISK_SCORES: commit=3 push=3 release=3
RISK_BYPASS: reducing
