I have enough context. This is a straightforward dev-tooling-only ESLint config change.

## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

Based on recent commits, the unreleased queue includes documentation changes (ADR 021, README restructure) and a changeset for a patch release (dep upgrades + waychaser fix). These are low-risk documentation and maintenance items.

- Scope: Documentation updates, changeset for patch release
- Risks: Minimal -- documentation and release metadata only
- **Residual risk: 1/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

No unpushed commits beyond the staged change described below. The unreleased queue is the only content.

- Scope: Same as Layer 1
- Additional risks from unpushed changes: None
- **Cumulative residual risk: 1/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

The uncommitted change is a single file: `eslint.config.js`.

- Removed the `'unicorn/no-array-for-each': 'off'` override (rule had 0 violations, so removing the override is a no-op)
- Added an explanatory comment to an existing rule override

#### Risk 1: ESLint config cleanup

- Inherent impact: 1/5 (Negligible) - Dev tooling config only. No runtime, build, publish, or deployment path affected. Per RISK-POLICY.md Impact Level 1: "Changes to... developer tooling configuration that do not affect the build, publish, or runtime paths."
- Inherent likelihood: 1/5 (Rare) - Removing an override for a rule with 0 violations is definitionally a no-op. Adding a comment has zero behavioral effect.
- Inherent risk: 1/25 (Very Low)
- Controls:
  - The rule had 0 violations before removal, so lint will pass identically after removal
  - Comment additions have no semantic effect on ESLint configuration
- **Residual risk: 1/25 (Very Low)**

- Scope: ESLint config housekeeping (1 file, dev-tooling only)
- Additional risks from uncommitted changes: None beyond Risk 1
- **Cumulative residual risk: 1/25 (Very Low)**

### Pipeline Summary

| Layer         | Scope                         | Residual Risk   |
| ------------- | ----------------------------- | --------------- |
| Unreleased    | Docs, changeset, dep upgrades | 1/25 (Very Low) |
| + Unpushed    | No additional                 | 1/25 (Very Low) |
| + Uncommitted | ESLint config cleanup (no-op) | 1/25 (Very Low) |

This is a risk-neutral change -- removing dead config and adding documentation comments.

RISK_SCORES: commit=1 push=1 release=1
RISK_BYPASS: reducing
