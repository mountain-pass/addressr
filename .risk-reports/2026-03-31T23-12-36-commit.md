I already have the current state of the files. The key question is what was removed. The user states that 17 UI-specialist hooks (a11y, voice-tone, style-guide, jtbd, review-plan-enforce) were disabled. Looking at the current settings.json, these hooks are already absent -- the staged diff has removed them. The remaining hooks are all infrastructure/risk/architect hooks.

Let me verify there are no application code changes and confirm the scope.

## Pipeline Risk Report

### Layer 1: Unreleased Changes (release risk)

**Scope:** Recent commits include risk-scorer refactoring, deterministic score writing, lifecycle tests, and hook infrastructure improvements (commits 5dff8bc through 7b82814). Plus the staged changes below.

No application code or production-affecting changes in the unreleased queue. All changes are developer tooling and governance infrastructure.

- **Residual risk: 1/25 (Very Low)**

### Layer 2: Unreleased + Unpushed (push risk)

**Scope:** Same as Layer 1 -- recent commits are on master and appear to already be the unpushed set.

- Additional risks from unpushed changes: None beyond Layer 1.
- **Cumulative residual risk: 1/25 (Very Low)**

### Layer 3: Unreleased + Unpushed + Uncommitted (commit risk)

**Scope:** Two staged files:

1. `.claude/settings.json` -- Removal of 17 UI-specialist hooks (accessibility-lead gate, voice-tone, style-guide, jtbd, review-plan-enforce) that are not relevant to backend-only work. Core governance hooks (architect-detect, architect-enforce-edit, risk-score, secret-leak-gate, wip-risk-gate, git-push-gate, risk-score-commit-gate, plan-risk-guidance, retrospective-reminder) all remain active.

2. `docs/BRIEFING.md` -- Minor editorial cleanup removing one stale line.

#### Risk 1: Reduced accessibility enforcement if UI work is attempted
- Inherent impact: 2/5 (Minor) -- accessibility hooks only affect developer workflow, not production. If UI work were attempted without these hooks, the CLAUDE.md instructions and global hooks still enforce accessibility review.
- Inherent likelihood: 1/5 (Rare) -- the stated context is backend-only dependency upgrade work; no UI files are being touched.
- Inherent risk: 2/25 (Very Low)
- Controls:
  - CLAUDE.md accessibility instructions remain in place and are checked by the global `~/.claude/` hooks (UserPromptSubmit proactive detection, PreToolUse edit gate) -- these are not in the project settings.json and are unaffected by this change.
  - The hooks can be re-enabled when UI work resumes (reversible change).
- **Residual risk: 1/25 (Very Low)**

#### Risk 2: Stale BRIEFING.md edit introduces misinformation
- Inherent impact: 1/5 (Negligible) -- BRIEFING.md is developer documentation only.
- Inherent likelihood: 1/5 (Rare) -- removing one stale line is trivial.
- Inherent risk: 1/25 (Very Low)
- **Residual risk: 1/25 (Very Low)**

- Additional risks from uncommitted changes: None material.
- **Cumulative residual risk: 1/25 (Very Low)**

### Pipeline Summary

| Layer | Scope | Residual Risk |
|-------|-------|--------------|
| Unreleased | Tooling/governance commits (risk-scorer, hooks, lifecycle tests) | 1/25 (Very Low) |
| + Unpushed | Same | 1/25 (Very Low) |
| + Uncommitted | Disable 17 UI hooks for backend work + BRIEFING.md cleanup | 1/25 (Very Low) |

### Assessment

This change is **risk-reducing**: it removes unnecessary hook overhead that would generate false blocks during backend-only work, improving developer velocity. No application code, production behavior, or core governance hooks are affected. The architect and risk-scorer enforcement chains remain fully intact.

RISK_SCORES: commit=1 push=1 release=1
RISK_BYPASS: reducing
